Source spec: `context/feature-specs/37-node-comments.md`

## Analyst Brief

### Scope statement

Add async, node-scoped comment threads — a flat, chronological, persisted list of comments per canvas node, discoverable via a small badge/trigger on the node itself and viewable/addable through a popover — distinct from live cursors/presence (spec 19) and the room-wide `ai-chat` feed (spec 25). Nothing else changes.

### Concrete deliverables

- `liveblocks.config.ts` (modified) — the room's `Storage` type gains a second field, `nodeComments: LiveList<NodeComment>`, alongside the existing `messages: LiveList<AiChatMessage>` (spec 25) — same flat-`LiveList`-per-feed shape, not a new `LiveMap`-per-node structure. Every entry carries its own `nodeId`, so one list serves every node in the room, matching the raw spec text exactly.
- `types/tasks.ts` (modified, additive) — `NodeCommentSchema` (Zod): `{ id, nodeId, sender, content, timestamp }` — the same field shape as `AiChatMessageSchema` plus `nodeId`, kept as a fully independent schema/type with **no shared base type** with `AiChatMessage` (matching this codebase's existing convention of independent per-feed types, e.g. `AiStatusMessage` vs. `AiChatMessageSchema` staying unrelated). Also exports the inferred `NodeComment` type and a `SendNodeComment` function-shape type (`(nodeId: string, content: string) => void`), mirroring `SendChatMessage`'s own doc/contract conventions (synchronous, throws on invalid outgoing data).
- `hooks/use-node-comments.ts` (new) — three exports:
  - `useNodeComments()`: mirrors `hooks/use-ai-chat-feed.ts#useAiChatFeed`'s exact internal shape (reads `root.nodeComments` via `useStorage`, validates every entry through `NodeCommentSchema.safeParse` inside a `useMemo` (dropping invalid entries, never rendering/crashing on one), and builds `sendComment` via `useCallback` wrapping a `useMutation`-built push, with `sender` resolved from a narrowly-selected `useSelf((me) => me.info.name)` (the same narrow-selector convention `useAiChatFeed` itself uses, and for the same documented reason — an unselected `useSelf()` re-renders, and gives the returned function a new identity, on every presence/cursor update). Returns `{ comments: NodeComment[]; sendComment: SendNodeComment }`. Called exactly once, inside `CanvasFlow` — see Open Questions #3 for why this is not parameterized per-node and not called directly inside every `CanvasNode` instance.
  - `NodeCommentsContext` — `React.createContext<{ comments: NodeComment[]; sendComment: SendNodeComment } | null>(null)`, structurally mirroring `hooks/use-update-canvas-node.ts`'s `CanvasNodeUpdateContext`/`hooks/use-canvas-search-highlight.ts`'s `CanvasSearchHighlightContext` pair — context provided by `CanvasFlow`, consumed by a leaf component, per `architecture-context.md`'s Hooks Convention.
  - `useNodeCommentsForNode(nodeId: string)`: the leaf-facing consumer hook, called by `NodeCommentsPopover`. Reads `NodeCommentsContext`, `useMemo`-filters the full `comments` list down to just this `nodeId` (client-side filtering happens here, not inside `useNodeComments()` itself — see Open Questions #3), and returns a nodeId-bound `sendComment(content: string)` plus the filtered `comments`. Returns an empty list and a throwing stub `sendComment` when called with no provider in the tree (mirroring `chatNotReadyYet`'s "not ready yet" contract in `ai-architect-tab.tsx`, not `useCanvasSearchHighlight`'s silent-`null` default — a comment send attempted with no real mutation behind it should surface as a genuine failure, not a silent no-op).
- `components/editor/canvas.tsx` (modified) —
  - `RoomProvider`'s `initialStorage` gains `nodeComments: new LiveList([])`, matching exactly how `messages` was added for spec 25.
  - `CanvasFlow` calls `useNodeComments()` once (valid here for the same reason `useAiChatFeed()`/`useAiStatusFeed()` already are — `CanvasFlow` sits inside `RoomProvider`) and wraps its returned `{ comments, sendComment }` in a new `<NodeCommentsContext.Provider>`, nested alongside the existing `CanvasNodeUpdateContext`/`CanvasEdgeUpdateContext`/`CanvasSearchHighlightContext` providers (spec 36's own precedent for a fourth nested provider). No push-up to `WorkspaceShell` — like search-highlight state, node comments have no consumer outside the room-bounded `CanvasFlow` subtree.
- `components/editor/node-comments-popover.tsx` (new) — `NodeCommentsPopover({ nodeId }: { nodeId: string })`:
  - A trigger element (badge/popover-trigger combined, per the raw spec's own "badge/popover trigger" phrasing) built on the existing `components/ui/popover.tsx` primitive (`Popover`/`PopoverTrigger`/`PopoverContent`, added by spec 36) — reused as-is, not modified, not duplicated with a second popover-like primitive (see Open Questions #2).
  - `PopoverContent` renders the node's own comment thread in chronological order (from `useNodeCommentsForNode(nodeId)`), each entry as a small bubble showing sender + formatted timestamp + content — mirroring `ChatBubble`'s sender/timestamp/content *layout convention* from `ai-architect-tab.tsx` (per the raw spec's own instruction), not a literal reuse of the `ChatBubble` component itself (see Open Questions #6), plus a small reply input (`Textarea` + Send `Button`, Enter-without-Shift submits) calling `sendComment(content)`.
  - An empty-thread state ("No comments yet.") when the node has zero comments, since the reply input must still be reachable to write the first one (see Open Questions #1).
- `components/editor/canvas-node.tsx` (modified, minimal) — renders `<NodeCommentsPopover nodeId={id} />` as one more sibling inside the existing `group relative` wrapper `<div>` (the same wrapper `ShapeVisual`, the four connection `Handle`s, and spec 36's highlight-ring class already share) — the *only* change this spec makes to this file, per its own explicit Scope Limit. See Open Questions #4 for exact placement/visual treatment.

### Acceptance criteria

1. `liveblocks.config.ts`'s `Storage` type gains `nodeComments: LiveList<NodeComment>`, alongside the existing `messages` field — same flat `LiveList`-per-feed shape as `ai-chat`.
2. `types/tasks.ts` defines `NodeCommentSchema` (Zod) with `{ id, nodeId, sender, content, timestamp }` and its inferred `NodeComment` type — the same field shape as `AiChatMessageSchema` plus `nodeId`, with no shared base type between the two schemas.
3. `hooks/use-node-comments.ts#useNodeComments()` reads/validates the `nodeComments` `LiveList` via `useStorage`, drops any entry that fails `NodeCommentSchema.safeParse` (never renders/crashes on an invalid entry), and exposes `sendComment(nodeId, content)` built via `useMutation` with `sender` resolved from `useSelf` — mirroring `use-ai-chat-feed.ts#useAiChatFeed`'s exact internal shape.
4. `RoomProvider`'s `initialStorage` in `components/editor/canvas.tsx` includes `nodeComments: new LiveList([])`.
5. A node with one or more comments shows a persistent, visible indicator (a small count badge) rendered from `canvas-node.tsx`.
6. Clicking the trigger opens a `Popover` (`components/ui/popover.tsx`, the existing primitive — no new/duplicate popover component) scoped to that node's own comment thread, rendered in chronological order.
7. Comments on different nodes never mix in the same thread view — the thread rendered for node A never shows an entry whose `nodeId` is not A's own id.
8. The popover includes a reply input; submitting calls `sendComment(nodeId, content)`, and the new comment persists in Liveblocks Storage and replays correctly to a participant who joins the room afterward.
9. A node with zero comments still has a reachable way to start its own first thread (the trigger itself is always present/clickable, not only the count badge) — see Open Questions #1 for why the raw spec's literal "badge only when count ≥ 1" wording needed this resolution.
10. `canvas-node.tsx`'s existing drag, resize, label-edit, select, and connection-handle behavior is unmodified — the only diff in that file is the new `<NodeCommentsPopover>` addition.
11. No threaded replies/replies-to-replies UI, no @mentions or notification mechanism, and no edge-comment capability exist anywhere in this diff.
12. `ai-chat` (`hooks/use-ai-chat-feed.ts`, the `messages` Storage field) and `ai-status-feed` are byte-for-byte untouched by this diff.
13. `npm run build` (and this repo's standard mechanical gate — `npx tsc --noEmit`, `npx eslint`, `npx vitest run --no-file-parallelism`) all pass.

### Dependencies

- **Spec 25 (Sidebar Chat Feed)** — the direct precedent this spec is explicitly told to follow for feed shape/persistence: `types/tasks.ts`'s Zod-schema convention, `liveblocks.config.ts`'s `Storage` type, and `hooks/use-ai-chat-feed.ts`'s `useStorage`/`useMutation`/`useSelf` pattern. This spec's own hook and schema are structural mirrors of these. Complete.
- **Spec 19 (Presence Avatars & Cursor)** — the contrast point named in the raw spec's own first line ("distinct from live cursors/presence"); not a functional dependency, cited only to confirm this feature must not route through Presence/broadcast. Complete.
- **Spec 11 (Base Canvas)** — `types/canvas.ts`'s `CanvasNode`/node `id` shape this spec's badge attaches to. Complete.
- **Spec 14 (Node Label Editing) / Spec 16 (Edge Behavior)** — establish `CanvasNode`'s `group relative` wrapper and the "context provided by `CanvasFlow`, consumed by the leaf `CanvasNode` renderer" architectural pattern (`CanvasNodeUpdateContext`) this brief's `NodeCommentsContext` directly mirrors. Complete.
- **Spec 36 (Canvas Node Search)** — introduces `components/ui/popover.tsx` (reused directly by this spec, not modified) and re-establishes the same Context-fan-out-from-`CanvasFlow` pattern (`CanvasSearchHighlightContext`) this brief's design follows a third time. Complete.

All dependencies are complete per `progress-tracker.md`.

### Open questions

1. **How does a node with zero comments ever get its first comment?** The raw spec text says the badge shows "when it has one or more comments," but names no other entry point for starting a thread — taken literally, a node at zero comments would have no reachable UI to write the first one.
   **Recommendation:** the trigger element is always present and clickable (the badge and the popover trigger are the same element, per the spec's own "badge/popover trigger" phrasing) — at zero comments it renders icon-only, hidden at rest and revealed on node hover (the exact same `opacity-0 group-hover:opacity-100` convention `canvas-node.tsx` already uses for its four connection handles); once the node has ≥1 comment, it becomes unconditionally visible (`opacity-100`) with a numeric count badge — satisfying acceptance criterion 5's "persistent visible indicator" literally, while still leaving a reachable way to start a first thread.

2. **Should the new component reuse spec 36's `Popover` primitive, or a different shadcn component?** Not addressed by the raw spec text.
   **Recommendation:** reuse `components/ui/popover.tsx` as-is (a protected foundation component per `ai-workflow-rules.md` — used, not redesigned or duplicated). It already provides exactly the "small trigger + anchored floating content" shape this feature needs; adding a second, functionally-near-identical popover primitive (or reaching for `Dialog`, which this codebase reserves for centered/modal-scale content like `ShareDialog`/`StarterTemplatesModal`) would be an unjustified duplication.

3. **Where should comment filtering-by-`nodeId` live: inside a parameterized `useNodeComments(nodeId)`, or as a full-list export with filtering left to the caller?** The raw spec text explicitly leaves this as "implementer's choice."
   **Recommendation:** full-list export, filtered by the caller — `useNodeComments()` (no parameter) is called exactly once, inside `CanvasFlow` (mirroring `useAiChatFeed()`'s own single call site), and its result is distributed to every `CanvasNode` leaf via a new `NodeCommentsContext` — the same single-subscription-then-Context-fan-out architecture this file already uses three times (`CanvasNodeUpdateContext`, `CanvasEdgeUpdateContext`, `CanvasSearchHighlightContext`). The alternative — a parameterized `useNodeComments(nodeId)` called directly inside each of N node instances — is technically legal (`CanvasNode` already renders inside the `RoomProvider` boundary) but would mean N independent `useStorage` subscriptions each re-validating the *entire* comment list on every change, an inefficiency this codebase has already shown active awareness of and fixed once before (`use-ai-chat-feed.ts`'s own narrowed-`useSelf`-selector change, made specifically to stop a re-render cascade). A thin `useNodeCommentsForNode(nodeId)` consumer hook (in the same file) does the actual per-node `useMemo` filter against the Context's already-validated list, keeping `NodeCommentsPopover`'s own call site a single line.

4. **Exact badge/trigger placement and visual treatment on `canvas-node.tsx`.** Not addressed by the raw spec text.
   **Recommendation:** `absolute -top-2 -right-2 z-20` on the node's existing `group relative` wrapper (the same wrapper `NodeColorToolbar`, the connection handles, and spec 36's highlight ring already attach to) — a small `rounded-full`/`bg-elevated`/`border-surface-border` pill (matching the edge-label-pill and `+N`-overflow-chip visual language already established elsewhere in this codebase) containing a `MessageCircle` icon (`h-3 w-3`) and, once count ≥ 1, the numeric count in `text-copy-primary`. Carries `nodrag nopan` so clicking it never starts a node drag. **Known, disclosed, non-blocking overlap:** `NodeResizer`'s own top-right corner resize handle occupies roughly the same corner when a commented node is *also* currently selected — both affordances are independently correct in their own gating condition (comment count vs. `selected`), and this is a Dev-level z-index/offset call to make both remain clickable, not a design defect to route back through this brief.

5. **How should the popover's open/close state be scoped: per-node local state, or a single "which node's thread is open" piece of state owned by `CanvasFlow` (mirroring spec 36's highlight-state scoping)?**
   **Recommendation:** per-node local state, owned by `NodeCommentsPopover` itself (the `Popover` primitive's own local open state). Unlike spec 36's search-highlight state — which is inherently a single, cross-node value (only one node can be "the current jump target" at a time, computed by an imperative handler in `CanvasFlow`) — a node's own comment-thread open/closed state has no cross-node coordination requirement: opening node A's thread has no bearing on node B's. Centralizing it in `CanvasFlow` would add a state-lifting indirection (and route every reply-input keystroke back up through a context) for no behavioral benefit.

6. **Literal `ChatBubble` reuse vs. mirroring only its layout convention.** The raw spec says "reuse `ChatBubble`'s sender/timestamp/content layout convention," but `ChatBubble` branches its left/right alignment and border/background color on `AiChatMessage.role` (`"user" | "assistant"`) — a field `NodeCommentSchema` deliberately does not have (per its own "same field shape as `AiChatMessageSchema` plus `nodeId`," listing only `{ id, nodeId, sender, content, timestamp }`).
   **Recommendation:** a small, new, uniformly-styled bubble defined locally in `node-comments-popover.tsx` — sender name + formatted timestamp header row, content below, `rounded-2xl`/`border-surface-border`/`bg-subtle`/`text-copy-primary`, left-aligned for every entry (no role-based split, since there is no role concept for a comment) — genuinely mirroring `ChatBubble`'s visual *convention* as the spec's own text asks, without importing or repurposing a component built around a field this schema doesn't carry.

### Out-of-scope callouts

- **Threaded replies / replies-to-replies** — explicit Scope Limit in the raw spec text; a flat, chronological list per node only.
- **@mentions or any notification mechanism** — explicit Scope Limit; the raw spec text itself points at "the 'notify on completion' idea" (spec 38, `context/feature-specs/38-completion-notifications.md`, still an unstarted raw draft) as a separate, later concern — not pulled forward into this spec.
- **Comments on edges** — explicit Scope Limit; nodes only, no `CanvasEdgeData` involvement.
- **Any change to `ai-chat` or `ai-status-feed`** — explicit Scope Limit; both stay byte-for-byte untouched (acceptance criterion 12).
- **Any change to `canvas-node.tsx`'s drag/select/edit behavior beyond the badge/popover trigger** — explicit Scope Limit; the only permitted diff is the new `<NodeCommentsPopover>` addition.
- **Comment editing or deletion** — not requested anywhere in the raw spec text; an append-only `LiveList`, matching `ai-chat`'s own append-only precedent exactly, with no update/delete mutation or UI.
- **A new `app/api` route or Prisma model** — comments live entirely in Liveblocks Storage, already governed by the existing room-token auth (`architecture-context.md`: "Liveblocks room tokens are issued only after verifying project membership"); no server-side persistence layer is added by this spec.
- **Billing, enterprise permission tiers, versioned spec history, production object storage migration, mobile apps** — `project-overview.md`'s Out of Scope wall; none of this spec's work touches any of them.

Brief ready for Senior Developer at `context/spec-status/37-node-comments.md`.

## Dev Notes

Branch: `spec/37-node-comments` (already checked out, off `main` — the branch tip at the start of this pass was `9dd266b`, "Merge pull request #34 from ravindrakamble/spec/36-canvas-search," so specs 33–36 are already in this branch's history). No new branch created.

### Files added

- `hooks/use-node-comments.ts` — `useNodeComments()` (mirrors `hooks/use-ai-chat-feed.ts#useAiChatFeed`'s exact internal shape: narrow `useSelf((me) => me.info.name)` selector, `useStorage((root) => root.nodeComments)` plus a `useMemo` validation pass dropping any entry that fails `NodeCommentSchema.safeParse`, a `useMutation`-built `pushComment`, and a `sendComment(nodeId, content)` `useCallback` that validates before mutating and throws synchronously on invalid outgoing data). `NodeCommentsContext` (`createContext<UseNodeCommentsResult | null>(null)`), structurally mirroring `hooks/use-update-canvas-node.ts`'s `CanvasNodeUpdateContext`/`hooks/use-canvas-search-highlight.ts`'s `CanvasSearchHighlightContext` pair. `useNodeCommentsForNode(nodeId)` — the leaf-facing consumer: `useMemo`-filters the context's full `comments` list to just this `nodeId`, and returns a nodeId-bound `sendComment(content)`; with no provider in the tree, returns `[]` and a throwing stub (`commentsNotReadyYet`, mirroring `ai-architect-tab.tsx#chatNotReadyYet`'s "not ready yet" contract), not a silent no-op.
- `hooks/use-node-comments.test.tsx` — 10 tests: `useNodeComments` (subscribes/returns valid comments in order, drops invalid entries including a foreign/`ai-chat`-shaped payload and a missing-`nodeId`/empty-`content` entry, empty-Storage no-crash, `sendComment` validates+pushes with `useSelf`-resolved `sender` and a per-node `nodeId`, throws-before-mutating on empty content, distinct ids per call, `nodeId`-scoping) and `useNodeCommentsForNode` (filters by `nodeId`, binds `sendComment` to that `nodeId`, empty-list-plus-throwing-stub with no provider). `.tsx` (not `.ts`) because the context-provider tests render JSX directly.
- `components/editor/node-comments-popover.tsx` — `NodeCommentsPopover({ nodeId })`: a combined badge/trigger button (`PopoverTrigger` built on `components/ui/popover.tsx`, reused as-is) — icon-only (`MessageCircle`, `h-3 w-3`) and hidden at rest (`opacity-0 group-hover:opacity-100`, the same connection-handle hover convention) at zero comments, unconditionally visible (`opacity-100`) with a numeric count badge once ≥1. `absolute -top-2 -right-2 z-20`, `nodrag nopan` so clicking it never starts a node drag, styled as a small `rounded-full`/`bg-elevated`/`border-surface-border` pill. `PopoverContent` renders the node's thread (`CommentBubble` — a small new local component: sender/timestamp header row + content, `rounded-2xl`/`border-surface-border`/`bg-subtle`/`text-copy-primary`, left-aligned for every entry since `NodeComment` has no `role` to branch on — mirroring `ChatBubble`'s layout convention, not a literal reuse) in chronological order, an empty-thread "No comments yet." state, and a reply `Textarea` + Send `Button` (Enter-without-Shift submits, Shift+Enter inserts a newline, disabled on empty/whitespace-only draft).
- `components/editor/node-comments-popover.test.tsx` — 8 tests: zero-comment trigger is icon-only/hover-revealed with no digit content, ≥1-comment trigger is persistently visible with the correct count, `nodrag`/`nopan` present on the trigger, empty state shows "No comments yet." with a reachable input, thread renders in the given (chronological) order scoped to the mocked per-node result, Send click calls `sendComment(content)` and clears the draft, Enter-without-Shift submits while Shift+Enter doesn't, empty/whitespace draft never submits (button `disabled`, Enter no-ops). `useNodeCommentsForNode` is mocked (`vi.importActual` for everything else in the module) so this file verifies the popover's own rendering/interaction logic independently of the hook's own already-covered filtering behavior.

### Files changed

- `liveblocks.config.ts` — `Storage` type gains `nodeComments: LiveList<NodeComment>` alongside the existing `messages: LiveList<AiChatMessage>` — same flat-`LiveList`-per-feed shape, not a `LiveMap`-per-node structure, per the brief.
- `types/tasks.ts` — new `NodeCommentSchema` (Zod: `{ id, nodeId, sender, content, timestamp }`), inferred `NodeComment` type, and `SendNodeComment` (`(nodeId: string, content: string) => void`) function-shape type — a fully independent schema/type from `AiChatMessageSchema`/`AiChatMessage`, no shared base type (per the brief, since `NodeComment` has no `role` field). No existing export in this file touched.
- `components/editor/canvas.tsx` — `RoomProvider`'s `initialStorage` gains `nodeComments: new LiveList([])`, matching exactly how `messages` was added for spec 25. `CanvasFlow` calls `useNodeComments()` once (valid here for the same reason `useAiChatFeed()`/`useAiStatusFeed()` already are) and wraps its returned `{ comments, sendComment }` in a new `<NodeCommentsContext.Provider>`, nested as a fourth provider alongside the existing `CanvasNodeUpdateContext`/`CanvasEdgeUpdateContext`/`CanvasSearchHighlightContext` — no push-up to `WorkspaceShell` (no `CanvasProps`-level change), since node comments have no consumer outside the room-bounded `CanvasFlow` subtree, same as spec 36's search-highlight state.
- `components/editor/canvas-node.tsx` — the only change permitted by the brief's Scope Limit: `<NodeCommentsPopover nodeId={id} />` added as one more sibling inside the existing `group relative` wrapper `<div>` (after the four connection `Handle`s). No other line in this file changed — independently re-checked via `git diff` that drag/resize/label-edit/select behavior is byte-for-byte unchanged.
- `components/editor/canvas-node.test.tsx` — the `cssShapes` "renders %s as a CSS div, not an SVG" test now scopes its `querySelector("svg")` check to `ShapeVisual`'s own root (`getShapeRoot(container)`) rather than the whole node wrapper, since `NodeCommentsPopover`'s trigger now always renders its own `MessageCircle` SVG icon as a further sibling in that wrapper — the SVG-absence assertion is about shape rendering specifically, not the wrapper as a whole. Added one new test (comments trigger renders as a wrapper sibling even with no `NodeCommentsContext` provider in the tree, matching acceptance criterion 9).
- `components/editor/canvas.test.tsx` — added a mocked `useNodeComments` (`@/hooks/use-node-comments`, `vi.importActual` for `NodeCommentsContext`/`useNodeCommentsForNode` so those stay real) with a default `{ comments: [], sendComment: vi.fn() }` return, mirroring `useAiChatFeedMock`'s own "verify wiring only" convention. Widened `roomProviderPropsRef`'s captured-props type and the `RoomProvider` mock's prop type to include `nodeComments`. New `describe("node comments (spec 37)", ...)` block: asserts `initialStorage.nodeComments` is an empty `LiveList` and that `useNodeComments` is called (subscribed inside the room boundary).

### Skills used

None of the installed `.claude/skills/` entries applied directly — `liveblocks-best-practices` was consulted for general Storage/`useMutation`/narrow-selector conventions, but this spec's actual Liveblocks Storage pattern (a flat `LiveList` per feed, validated via Zod on read, `useMutation`-built writer) is a direct structural mirror of this codebase's own already-shipped `hooks/use-ai-chat-feed.ts` (spec 25), which the brief explicitly directs following — no new pattern was introduced that needed the skill's own guidance beyond what spec 25 already established. No Prisma/Trigger.dev/Clerk surface touched.

### Key decisions

- Followed every one of the brief's Open Questions recommendations as written (all six): trigger-always-present with hover-reveal at zero comments (#1); reuse `components/ui/popover.tsx` unmodified (#2); full-list `useNodeComments()` called once in `CanvasFlow`, filtered per-node by `useNodeCommentsForNode` via Context (#3); `absolute -top-2 -right-2 z-20` badge/trigger placement with the documented, accepted `NodeResizer` corner-handle overlap when a commented node is also selected (#4); per-node local `Popover` open state, not centralized in `CanvasFlow` (#5); a new local `CommentBubble` mirroring `ChatBubble`'s layout convention rather than importing/repurposing that component (#6).
- `useNodeCommentsForNode`'s "no provider" throwing stub calls a dedicated `commentsNotReadyYet(): never` helper (mirroring `ai-architect-tab.tsx`'s `chatNotReadyYet`/`agentChatNotReadyYet` naming convention) rather than inlining `throw new Error(...)` at the call site — keeps the "why" documented in one place and TypeScript's control-flow narrowing (a `never`-returning call terminates the branch) lets the following `context.sendComment(...)` line access `context` without a redundant non-null assertion.
- `NodeCommentsPopover`'s reply `Textarea`/Send `Button` sizing (`min-h-[52px] max-h-[120px]`, `size="icon"` button) is a Dev-level choice scaled down from `ai-architect-tab.tsx`'s own input row (`min-h-[72px] max-h-[160px]`) to fit a popover's much smaller footprint — not pinned by the brief, which only specifies the interaction contract (Textarea + Send Button, Enter-without-Shift submits), not exact sizing.

### Test coverage

New/extended: `hooks/use-node-comments.test.tsx` (new, 10 tests), `components/editor/node-comments-popover.test.tsx` (new, 8 tests), `components/editor/canvas-node.test.tsx` (+1 test, one existing test's assertion scope narrowed for the new sibling SVG icon), `components/editor/canvas.test.tsx` (+2 tests in a new `describe` block, mock surface extended). No dedicated `types/tasks.ts` schema-only test file — `NodeCommentSchema`'s validation behavior is exercised indirectly through `use-node-comments.test.tsx`'s drop-invalid-entries and throw-on-invalid-outgoing-data tests, mirroring this codebase's existing precedent of not unit-testing `AiChatMessageSchema` standalone either.

Commands run, all green:
- `npx tsc --noEmit` — clean.
- `npx eslint .` (full repo) — one pre-existing error (`components/editor/editor-shell.tsx`, `react-hooks/set-state-in-effect`) and one pre-existing warning (an unrelated `.agents/skills/` template file), both independently confirmed via `git status --porcelain` to be outside this diff (neither file appears in the changed/added file list above) — this spec's own files are individually clean.
- `npx vitest run --no-file-parallelism` — 858/858 tests passing across 83/83 files. The branch tip at the start of this pass already includes spec 36's merged work (837/81, per that spec's own Dev Notes), so this pass's net contribution is +21 tests (10 + 8 new files, +1/+2 in the two modified test files) and +2 files, landing on 858/83. `progress-tracker.md`'s own "818/80 after spec 35" figure predates spec 36's merge into `main` and isn't this branch's actual starting point. One pre-existing jsdom console warning ("Not implemented: navigation to another Document") appears during the run, unrelated to this diff.
- `npx next build` — succeeds; no new route (this is a pure client-side/Liveblocks-Storage feature, no new `app/api` route or page, per the brief's own explicit "no new route or Prisma model" callout).

### Known limitations / deferrals

- No live browser/manual smoke test of the hover-reveal-trigger → open-popover → write-comment → persist → replay-to-a-later-joiner flow against a real running dev server with a real Liveblocks room was performed in this environment — recommended as a human smoke test, not a blocker; every acceptance criterion is verified at the unit/integration level via RTL against real component/hook code (the hook's own `useStorage`/`useMutation`/`useSelf` calls are mocked at the Liveblocks SDK boundary only, not the hook's own validation/filtering/mutation-building logic).
- Consistent with the brief's own Out-of-scope callouts, all honored as specified: no threaded replies (flat chronological list only, `NodeCommentSchema` has no parent/reply-to field), no @mentions or notification mechanism, no edge-comment capability (`CanvasEdgeData` is never referenced anywhere in this diff), `hooks/use-ai-chat-feed.ts` and `ai-status-feed` are byte-for-byte untouched (independently confirmed via `git diff` — zero-line diff on both), no comment editing/deletion (append-only `LiveList`, no update/delete mutation or UI), and no new `app/api` route or Prisma model (comments live entirely in Liveblocks Storage, governed by the room token auth already issued after project-membership verification).
- `canvas-node.tsx`'s diff is intentionally minimal per the brief's explicit Scope Limit — confirmed via `git diff` that the only change is the new import plus the single `<NodeCommentsPopover nodeId={id} />` line; every other line (drag, resize, label-edit, select, connection-handle behavior) is byte-for-byte unchanged.
- The brief's Open Questions #4 flags a known, accepted, non-blocking visual overlap between the new comment-trigger badge and `NodeResizer`'s own top-right corner resize handle when a commented node is also currently selected — both affordances are independently correct in their own gating condition (comment count vs. `selected`); not treated as a bug to fix in this pass, per the brief's own framing.

## QA Report

**Overall verdict: PASS**

### Mechanical gate

| Check | Result |
| --- | --- |
| `npx tsc --noEmit` | PASS -- clean, no errors |
| `npx eslint .` | PASS -- 0 errors/warnings in this diff's files. Full-repo run shows the same 1 pre-existing error (`components/editor/editor-shell.tsx`, `react-hooks/set-state-in-effect`) and 1 pre-existing warning (`.agents/skills/.../__root.tsx`) already documented by prior specs' QA passes; confirmed via `git diff 9dd266b ed37b61` that neither file appears in this branch's diff for spec 37 -- both are pre-existing and unrelated |
| `npx vitest run --no-file-parallelism` | PASS -- 858/858 tests, 83/83 files |
| `npx next build` | PASS -- succeeds; no new route added (no new `app/api` entry in the build's route list -- pure client-side/Liveblocks-Storage feature) |

All Dev Notes claims about the mechanical gate were independently reproduced and confirmed accurate.

### Acceptance criteria checklist

1. **PASS** -- `liveblocks.config.ts`'s `Storage` type gains `nodeComments: LiveList<NodeComment>` alongside the existing `messages: LiveList<AiChatMessage>` -- same flat-`LiveList`-per-feed shape, not a `LiveMap`-per-node structure.
2. **PASS** -- `types/tasks.ts` defines `NodeCommentSchema` (Zod) with exactly `{ id, nodeId, sender, content, timestamp }` and an inferred `NodeComment` type. Confirmed no shared base type with `AiChatMessageSchema`/`AiChatMessage` -- fully independent `z.object` definitions.
3. **PASS** -- `hooks/use-node-comments.ts#useNodeComments()` genuinely mirrors `use-ai-chat-feed.ts#useAiChatFeed`'s internal shape line-for-line: narrow `useSelf((me) => me.info.name)` selector, `useStorage((root) => root.nodeComments)` plus a `useMemo` validation pass that drops any entry failing `NodeCommentSchema.safeParse` (never renders/crashes on an invalid entry -- verified by `hooks/use-node-comments.test.tsx`'s drop-invalid-entries test, which includes a missing-`nodeId` entry, an empty-`content` entry, and a foreign/`ai-chat`-shaped payload with a stray `role` field), and a `useMutation`-built `pushComment` wrapped by a `sendComment(nodeId, content)` `useCallback` that validates before mutating and throws synchronously on invalid outgoing data.
4. **PASS** -- `RoomProvider`'s `initialStorage` in `components/editor/canvas.tsx` includes `nodeComments: new LiveList([])`, added identically alongside the existing `messages: new LiveList([])`.
5. **PASS** -- `NodeCommentsPopover`'s trigger renders a numeric count badge once `comments.length >= 1`, `opacity-100` (persistently visible), rendered as a sibling inside `canvas-node.tsx`'s wrapper.
6. **PASS** -- the trigger opens `components/ui/popover.tsx`'s `Popover`/`PopoverTrigger`/`PopoverContent` primitives, unmodified (zero-line diff on `popover.tsx` confirmed -- not in this diff's file list) and not duplicated. `PopoverContent` renders `useNodeCommentsForNode(nodeId)`'s filtered thread in chronological (array/push) order.
7. **PASS** -- `useNodeCommentsForNode` filters the full `comments` list by `comment.nodeId === nodeId` inside a `useMemo`; confirmed by both the hook-level test (filters the full comment list down to just the given nodeId) and the component-level test scoping only same-node comments into the rendered thread. No cross-node mixing possible -- the filter predicate is a strict equality check.
8. **PASS** -- the reply input's Send/Enter-without-Shift path calls `sendComment(content)`, which `useNodeCommentsForNode` binds to `context.sendComment(nodeId, content)` -- the real `useMutation`-backed Storage push in `useNodeComments()`. Because this writes to a Liveblocks Storage `LiveList` (not `broadcastEvent`), it persists and replays to a participant joining afterward, consistent with spec 25's already-established `ai-chat` precedent this hook mirrors exactly. No live/manual smoke test was performed (disclosed by the Dev as a non-blocking deferral); acceptance is satisfied at the unit/integration level, consistent with how this repo has qualified equivalent Liveblocks-Storage criteria in prior specs (e.g. spec 25/36).
9. **PASS** -- at zero comments the trigger renders icon-only, `opacity-0 group-hover:opacity-100` (hover-revealed, matching the existing connection-handle convention), and is confirmed always present in the DOM even with no `NodeCommentsContext` provider in the tree (`canvas-node.test.tsx`'s new node comments trigger spec 37 test). This closes the gap the Analyst Brief's Open Questions #1 flagged.
10. **PASS** -- `git diff 9dd266b ed37b61 -- components/editor/canvas-node.tsx` shows exactly one new import and one new `<NodeCommentsPopover nodeId={id} />` line; every other line (drag/resize/label-edit/select/connection-handle behavior) is byte-for-byte unchanged.
11. **PASS** -- no threading/replies-to-replies (flat array, `NodeCommentSchema` has no parent/reply-to field), no at-mentions/notification mechanism, and no edge-comment capability anywhere in the diff -- confirmed via `git diff --stat` showing zero touch to `components/editor/canvas-edge.tsx`/`types/canvas.ts`'s `CanvasEdgeData`, and no mention/notification-related code in `node-comments-popover.tsx` or `use-node-comments.ts` (grepped, no matches).
12. **PASS** -- `git diff 9dd266b ed37b61 -- hooks/use-ai-chat-feed.ts hooks/use-ai-status-feed.ts` returns zero lines -- both files are byte-for-byte untouched.
13. **PASS** -- all four mechanical gate commands reproduced independently above, all green.

### Architecture invariants

- No long-running AI work in a request handler -- no request handler is touched by this diff at all (pure client-side Liveblocks Storage feature, no new/changed `app/api` route). OK.
- Metadata vs. blob storage kept separate -- not applicable; no persistence outside Liveblocks Storage is added. OK.
- Auth/ownership enforced at every mutation boundary -- the `sendComment` mutation writes to the room's Storage `LiveList`, which is only reachable after the existing room-token auth (`app/api/liveblocks-auth/route.ts`, verified at room-join time) -- no new auth surface, consistent with `ai-chat`'s own precedent. OK.
- Client components used only where browser interactivity or real-time state requires them -- `use-node-comments.ts` and `node-comments-popover.tsx` are both correctly marked `"use client"`; this is inherently client-only, real-time state. OK.
- Canvas schema consistency between user-created content and imported templates -- untouched; `NodeComment` is not part of `CanvasNode`/`CanvasNodeData` and is not persisted through the canvas-save/template-import path at all. OK.
- No violation found.

### Standards compliance spot-check

- No raw Tailwind grayscale (`zinc-`/`slate-`/`gray-`/`neutral-`) or hex colors found in any new/changed file (`node-comments-popover.tsx`, `use-node-comments.ts`, `canvas.tsx`, `canvas-node.tsx`) -- grepped, zero matches. Existing tokens (`bg-elevated`, `border-surface-border`, `text-copy-primary`, `text-copy-muted`, `bg-subtle`) used throughout.
- No `any` in any new/changed line -- grepped `hooks/use-node-comments.ts` and `node-comments-popover.tsx`, zero matches; mutation callback types in the hook and in `use-node-comments.test.tsx`'s mock are explicitly typed.
- `components/ui/*` -- no file in that directory appears in this diff (`popover.tsx` reused unmodified, confirmed by its absence from the changed-file list).
- Hooks convention -- `NodeCommentsContext`/`useNodeCommentsForNode` genuinely follow the established Context-fan-out-from-`CanvasFlow` pattern (`CanvasNodeUpdateContext`, `CanvasSearchHighlightContext`), a single `useNodeComments()` subscription call site inside `CanvasFlow`, not N per-node `useStorage` subscriptions -- confirmed by reading `canvas.tsx`'s diff (one call site) and `canvas-node.tsx`/`node-comments-popover.tsx` (leaf only calls the parameterized consumer hook, never `useNodeComments()` directly).

### Error handling

- Invalid/malformed Storage entries (missing `nodeId`, empty `content`, foreign `ai-chat`-shaped payloads with a stray `role` field): dropped via `NodeCommentSchema.safeParse`, never rendered or crashing -- handled and test-covered.
- Empty Storage: `useNodeComments()` returns `comments: []` without crashing -- handled and test-covered.
- Outgoing invalid comment (empty content): `sendComment` throws synchronously before ever calling the `useMutation` push -- handled and test-covered (`pushSpy` confirmed not called).
- No `NodeCommentsContext` provider in the tree: `useNodeCommentsForNode` returns an empty list and a throwing `sendComment` stub (`commentsNotReadyYet()`), matching the briefs explicit "genuine failure, not silent no-op" requirement -- handled and test-covered, and also confirmed reachable via `canvas-node.test.tsx`'s no-provider render test (trigger still renders, it just cannot submit).
- Whitespace-only draft: Send button `disabled`, Enter no-ops -- handled and test-covered.
- Comments on different nodes never mixing: enforced by strict `nodeId` equality filtering, verified at both the hook and component test level.

This is a Liveblocks-Storage-only, client-side feature with no new network/auth surface of its own beyond the rooms already-established token auth; all failure modes implied by the spec are handled.

### Housekeeping

- `context/progress-tracker.md` updated: Current Goal now correctly describes spec 37 as implemented/awaiting QA (not yet Completed), and a new In Progress entry accurately summarizes what was actually built, matching the diff.

### Issues

None found. No Bug-to-Dev or Spec-gap-to-Analyst items -- the implementation matches the brief on every acceptance criterion, honors every scope limit, and the mechanical gate is fully green.

QA passed -- ready for Product Owner review.

## Product Owner Review (round 1)

**Verdict: PASS — ready for human review**

### Independent re-verification (not taken on Dev/QA's word)

Re-ran `git diff main...spec/37-node-comments --stat` and read the actual diffs of every file the Dev Notes claim, rather than trusting the narrative:

- `liveblocks.config.ts` — confirmed `nodeComments: LiveList<NodeComment>` is a genuine second flat `LiveList` field on `Storage`, alongside the untouched `messages: LiveList<AiChatMessage>` — not a `LiveMap`-per-node structure. Matches the raw spec text's explicit instruction (`context/feature-specs/37-node-comments.md`, Implementation §1) exactly.
- `hooks/use-node-comments.ts` — read in full. `sendComment` builds a candidate object, validates it with `NodeCommentSchema.safeParse` (throwing before ever touching Storage on failure), then calls `pushComment`, which is a `useMutation(({ storage }, comment) => { storage.get("nodeComments").push(comment) })`. This is a genuine Liveblocks **Storage** mutation — not `broadcastEvent`, not Presence. Storage is CRDT-backed and replicated to every client on room load, which is precisely the mechanism that makes "a participant joining mid-session sees the full comment history" true by construction, not by convention. This is the spec's own explicitly named core guarantee (raw spec's "Notes" section: "must replay to a participant who joins mid-session — Storage `LiveList`, not `broadcastEvent`") and it is genuinely satisfied, not merely asserted.
- `git diff main...spec/37-node-comments -- hooks/use-ai-chat-feed.ts hooks/use-ai-status-feed.ts` returned zero output — both files are byte-for-byte untouched, independently confirmed rather than trusting QA's claim of a zero-line diff. This is a real architectural boundary this app draws between presence/cursors (spec 19), room-wide AI chat (spec 25), and now node comments (spec 37) — three genuinely separate feeds, none of which reads or writes another's Storage field or broadcast channel.
- `components/editor/canvas-node.tsx` diff — exactly one new import (`NodeCommentsPopover`) and one new line, `<NodeCommentsPopover nodeId={id} />`, added as a sibling inside the existing `group relative` wrapper after the four connection `Handle`s. Every other line — drag, resize, label-edit, select, connection-handle logic — is unchanged. Confirmed directly, not from the Dev/QA narrative.
- `components/editor/canvas.tsx` diff (120 lines, the largest in the stat) — read in full. The apparent size is entirely re-indentation from wrapping the existing JSX subtree (`ReactFlow`, `ShapePanel`, `CanvasControlBar`, `StarterTemplatesModal`, `SaveTemplateDialog`, `PresenceAvatars`, `LiveCursors`) one level deeper inside a new `<NodeCommentsContext.Provider>`, plus one `useNodeComments()` call and one `initialStorage.nodeComments: new LiveList([])` addition. No existing JSX, prop, or callback logic changed — matches spec 36's own precedent for this same "fourth nested provider" pattern exactly.
- `types/tasks.ts` diff — `NodeCommentSchema` is a fully independent `z.object` (`{ id, nodeId, sender, content, timestamp }`), no shared base type or extension of `AiChatMessageSchema`, matching the brief's explicit design choice (no `role` field, since a comment has no user/assistant concept).

Everything Dev/QA claimed about these six files is genuinely present in the diff. No claim was taken at face value.

### Product fit against `project-overview.md`

- **Scope**: In Scope explicitly lists "Real-time shared canvas with nodes, edges, and presence." Node-scoped comment threads are new collaboration surface layered on the existing canvas/room, not a new system boundary — no new `app/api` route, no new Prisma model, no billing/quota/permission-tier concept, no versioned history, no object-storage migration, no mobile surface. Checked directly against the Out of Scope wall (billing, enterprise permission tiers, versioned spec history, production object storage migration, mobile apps) — this spec touches none of them. This is a legitimate, narrowly-scoped extension, not scope creep.
- **Success Criterion 2** ("Multiple users can collaborate in the same canvas simultaneously") — this spec strengthens it more directly than specs 35/36 did. Specs 35 (IaC export) and 36 (canvas search) are single-user conveniences layered on an already-collaborative canvas; this spec adds genuinely new *multi-user* collaboration surface — an asynchronous discussion mechanism scoped to a specific node, distinct from live cursors (momentary) and the AI chat feed (room-wide, not node-scoped). A team refining an architecture together now has a real way to leave context/questions attached to a specific decision point on the diagram, which is a substantive product capability for a "collaborative real-time canvas for system design" (Goal 2), not just a technicality that satisfies the brief's letter.
- **Goal 5** ("Let collaborators refine the generated architecture") — node comments give collaborators a durable, discoverable channel for the kind of back-and-forth ("why is this a queue and not direct calls?") that refining a design actually requires, without polluting the AI chat feed or forcing synchronous presence. This is a genuine product win, not a checkbox.
- No crossing into Out of Scope: no threaded replies, no @mentions/notifications (explicitly deferred to spec 38's own separate draft), no edge comments, no comment edit/delete — all independently confirmed absent from the diff (no `parent`/`replyTo` field in `NodeCommentSchema`, no touch to `canvas-edge.tsx`/`CanvasEdgeData`, append-only `LiveList` with no update/delete mutation).

### Rough edges — acceptable at this stage

- No live browser/manual smoke test of the full hover → open → write → persist → replay-to-later-joiner flow against a real running Liveblocks room (disclosed by Dev, confirmed by QA, and consistent with how prior specs in this same run — 33's Blob store, 34's `/share` route — have handled the same class of deferral). Worth a human smoke test before treating the mid-session-replay guarantee as fully proven in production, but the mechanism itself (Storage `LiveList`, not `broadcastEvent`) is verified at the code level above, and this is not the first spec in this run to defer a live-room smoke test to the human.
- The disclosed `NodeResizer` corner-handle / comment-badge visual overlap when a commented node is also selected is a known, non-blocking cosmetic edge case, not a functional defect — both affordances remain independently clickable per the brief's own Open Questions #4.
- Nothing here would block a later spec (e.g. spec 38's completion-notifications draft, which explicitly says it may build on this) from building on this one's contract: `NodeComment`'s shape, the `NodeCommentsContext`/`useNodeCommentsForNode` fan-out pattern, and the append-only `LiveList` convention are all clean, precedent-following surfaces to extend from.

### `progress-tracker.md` accuracy

The current "In Progress" entry for spec 37 accurately describes what was actually built and verified — the `LiveList<NodeComment>` Storage field, the schema shape, the `useNodeComments`/`NodeCommentsContext`/`useNodeCommentsForNode` architecture, the hover-reveal/persistent-badge trigger behavior, the single `<NodeCommentsPopover>` addition to `canvas-node.tsx`, the explicit non-features (no threading/mentions/edge-comments/edit-delete), and the 858/858 test count — all independently confirmed above, not aspirational. This entry will be moved to "Completed" as part of this review, per the standard process (PR opening/pushing is a separate step, not performed here).
