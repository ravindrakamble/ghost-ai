Source spec: `context/feature-specs/25-sidebar-chat-feed.md`

## Analyst Brief

### Scope statement

Add a second, separate Liveblocks mechanism — `ai-chat` — to the same room the canvas already uses, so participants can send and see ordered, persisted chat messages (sender, timestamp, content) in the AI sidebar's chat area, wired onto the existing sidebar input/Send button from spec 20. This unit is collaborative sidebar chat only: no AI-generated replies, no triggering of `/api/ai/*` routes or Trigger.dev tasks, and no touching of `ai-status-feed`/AI presence (spec 23/24's already-delivered territory).

### Concrete deliverables

- `types/tasks.ts` (modified, additive) — a real **Zod** schema for the chat message shape (`AiChatMessageSchema`, exporting an inferred `AiChatMessage` type via `z.infer`), covering the four fields the spec text names: `sender`, `role`, `content`, `timestamp`. This is the first spec in this codebase to use Zod — specs 22–24 deliberately used manual type guards for their own event payloads because their spec text never named a validation library, but spec 25's text names "Zod schema" twice, so a hand-rolled guard would not honestly satisfy this spec's literal ask (see Open Questions #1). Also exports a validation helper built on the schema (e.g. `AiChatMessageSchema.safeParse`) used both to filter invalid entries out of the rendered feed and, recommended, to validate an outgoing message before it's written to Storage.
- `package.json`/`package-lock.json` (modified) — add `zod` as a new dependency (confirmed genuinely missing from `package.json` today — see Open Questions #1).
- `hooks/use-ai-chat-feed.ts` (new, per `architecture-context.md`'s Hooks Convention) — wraps Liveblocks' `useStorage` to read the room's chat `LiveList`, validates each entry via the Zod schema (dropping anything invalid rather than crashing the sidebar), and returns the ordered, validated message array; also builds a `sendMessage` function on `useMutation` that validates the outgoing message against the same schema before appending it to the `LiveList`. Must be called from inside the room's `RoomProvider` boundary (same constraint spec 24's `useAiStatusFeed` already documents).
- `components/editor/canvas.tsx` (modified) — `RoomProvider` gains an `initialStorage` prop defining the new chat list's root key (e.g. `initialStorage={{ messages: new LiveList([]) }}`), since no `initialStorage` is set today (confirmed by reading the current `RoomProvider` call — it only passes `initialPresence`). `CanvasFlow` calls the new hook, pushes the validated message list up via a new `onChatMessagesChange` callback (mirroring spec 24's `onAiStatusChange` exactly for the "state flows up" direction), and receives/forwards the hook's `sendMessage` function back down as a new prop — the bidirectional counterpart spec 24 didn't need, since that spec only ever consumed a feed, never wrote to one (see Open Questions #3).
- `liveblocks.config.ts` (modified, recommended) — add a `Storage` type to the existing global `Liveblocks` interface augmentation (alongside `Presence`/`UserMeta`) for the new `messages: LiveList<AiChatMessage>` root key, matching this file's own established convention rather than leaving Storage untyped.
- `components/editor/workspace-shell.tsx` (modified) — owns new `chatMessages`/`sendChatMessage` state and callback wiring into `<Canvas>`, mirroring the existing `aiStatus` pattern exactly, then threads both down to `<AiSidebar>` as new props.
- `components/editor/ai-sidebar.tsx` (modified) — forwards the two new props straight through to `<AiArchitectTab>` — no new state owned here, same "pass the real mechanism down as props" posture spec 18/21/24 already established.
- `components/editor/ai-architect-tab.tsx` (modified — a genuine rewrite of this component's message-state ownership, not scope creep): replaces the local-only `useState<ChatMessage[]>` array and its `handleSubmit` push (spec 20) with the real, persisted `chatMessages` prop rendered through the existing `ChatBubble` component (extended to also display `sender` and a formatted `timestamp`, per the spec's "show sender, timestamp, and message content"), and a call to the passed-down `sendMessage` on submit. Clears the input only once the send call completes without error; on failure, preserves the input text and shows a small inline error indicator (recommend the `text-state-error` token, matching `SaveStatusIndicator`'s existing error convention). Starter-prompt-chip fill-not-submit behavior and Enter/Shift+Enter handling are untouched. Spec 20's own docblock already names this exact local array as something "spec 25 owns" — this replacement is the expected next step, not an unplanned rewrite.
- Tests: `types/tasks.test.ts` (extended — Zod schema accept/reject cases for all four fields), `hooks/use-ai-chat-feed.test.ts` (new — subscribes correctly, filters invalid Storage entries, `sendMessage` validates before mutating), `components/editor/ai-architect-tab.test.tsx` (extended — renders persisted messages with sender/timestamp/content, clears input on success, shows error state on failure), `components/editor/canvas.test.tsx`/`workspace-shell.test.tsx`/`ai-sidebar.test.tsx` (extended — new prop/callback threading).
- No changes to `trigger/design-agent.ts`, `lib/design-agent-ai.ts`, `lib/design-agent-room.ts`, `app/api/ai/design*`, `hooks/use-ai-status-feed.ts`, or any `ai-status-feed`/AI-presence code path in `live-cursors.tsx` — spec 23/24's already-delivered territory, untouched by this spec.

### Acceptance criteria

1. A chat `LiveList` (or equivalently named Storage root key) is added to the shared Liveblocks room's Storage, room-scoped, implemented via Liveblocks Storage — not `broadcastEvent` — per `architecture-context.md`'s Realtime Conventions, which pins `ai-chat` to a Storage `LiveList` specifically because it "must replay to participants who join mid-conversation."
2. `types/tasks.ts` exports a real Zod schema (not a hand-written type guard) for the chat message shape, covering at minimum `sender`, `role`, `content`, and `timestamp`, plus a TypeScript type inferred from that schema.
3. The AI sidebar's chat area subscribes to the room's chat Storage via Liveblocks' real `useStorage` mechanism (not polling, not a second websocket, not a parallel realtime system) and renders every message in order, showing sender, timestamp, and content for each message.
4. A user can send a message through the existing sidebar `Textarea`/Send button (no new input UI). On a successful send, the input clears. On a failed send, the input's contents are preserved and a small, visible error indicator appears.
5. Every message read from Storage is validated against the Zod schema before it is rendered; an invalid/malformed entry is dropped (not rendered), and does not crash or blank the rest of the sidebar.
6. `ai-chat` and `ai-status-feed` remain fully independent mechanisms — no shared payload type, no shared subscription, no cross-reading of one feed's data as the other's, and no chat message is ever pushed onto `ai-status-feed` or vice versa.
7. No AI-generated reply is produced anywhere in this diff — every message rendered through this spec's own send path carries `role: "user"`, and `role: "assistant"` remains schema-supported but unreachable through this spec's UI, matching spec 20's own precedent for `ChatBubble`'s assistant branch.
8. No `/api/ai/*` route and no Trigger.dev task is called, triggered, or added anywhere in this diff.
9. No new realtime system outside Liveblocks is introduced (no WebSocket, no Server-Sent Events, no polling loop).
10. `npm run build` passes, along with this repo's standard gates (`npx tsc --noEmit`, `npx eslint .`, `npx vitest run`).

### Dependencies

- **Spec 20 (AI Sidebar Shell)** — `components/editor/ai-sidebar.tsx`/`ai-architect-tab.tsx`, explicitly built "presentational only... specs 22/24/25/26/27/29's job to wire on top of this shell without redesigning it," and its own docblock explicitly reserving the real chat message schema for this spec. Also the pre-existing `ChatBubble` component and its `role: "user" | "assistant"` vocabulary, which this spec should reuse rather than reinvent. **Complete.**
- **Spec 10 (Liveblocks Setup)** — `liveblocks.config.ts`'s global `Liveblocks` type-augmentation convention (`Presence`/`UserMeta`) this spec extends with a `Storage` type. **Complete.**
- **Spec 11 (Base Canvas)** — establishes the `LiveblocksProvider`/`RoomProvider` boundary in `components/editor/canvas.tsx`, including the exact `RoomProvider` call this spec must add `initialStorage` to. **Complete.**
- **Spec 24 (AI Presence State)** — establishes the callback-push-up pattern (`onAiStatusChange`) this spec reuses for the "message list flows up out of the room boundary" half of its own wiring, and its own Out-of-scope callouts explicitly name `ai-chat`/persisted chat rendering/sender-timestamp display as "spec 25's job." **Complete** (QA PASS, Product Owner PASS, PR #20 open against `main`, not yet merged).
- **Spec 23 (Design Agent Logic)** — the producer of `ai-status-feed`, the feed this spec must stay structurally independent from. **Complete**, not otherwise touched by this spec.
- `architecture-context.md`'s Realtime Conventions section — explicitly pins `ai-chat` to a Storage `LiveList` (not `broadcastEvent`) ahead of this spec, resolving what would otherwise be this brief's most consequential open call. **Already resolved**, not a decision this brief is making unilaterally.

### Open questions

1. **Zod is not currently an installed dependency.** Confirmed by reading `package.json` directly — no `zod` entry exists anywhere in `dependencies` or `devDependencies`. Unlike specs 22/23/24 (which each deliberately used a manual runtime type guard instead of Zod, because their own spec text never named a validation library by name), spec 25's text explicitly says "define or reuse a Zod schema in `types/tasks.ts`" — naming Zod specifically, twice. Recommendation: install `zod` as a new dependency and use a real `z.object({...})` schema, not a manual guard relabeled as one — following the same "the spec's premise didn't hold, install what's actually needed" precedent specs 10/21/22/23 already set for `@liveblocks/node`/`@vercel/blob`/`@trigger.dev/sdk`/`@ai-sdk/google`. Flagging as a recommendation, not a unilateral decision, since it is a new production dependency.
2. **`RoomProvider` currently has no `initialStorage`.** Reading `components/editor/canvas.tsx` directly: `RoomProvider id={roomId} initialPresence={{ cursor: null, thinking: false }}` has no `initialStorage` prop, and no `Storage` type is declared in `liveblocks.config.ts` today — `useLiveblocksFlow` manages nodes/edges without needing a declared root shape. Adding a `messages` Storage root key for chat is additive (it does not touch how nodes/edges are read or written), but it is a change to the one `RoomProvider` instantiation every canvas-related spec since 11 depends on. Recommendation: add `initialStorage={{ messages: new LiveList([]) }}` (exact key name a Dev-level choice) and a matching `Storage` type in `liveblocks.config.ts` for consistency with the existing `Presence`/`UserMeta` convention. Flagging for the Senior Developer to confirm since it touches shared room-initialization code, not because the change itself is expected to be risky.
3. **`AiSidebar` still renders outside the Liveblocks room boundary, and this spec's wiring is now bidirectional.** Spec 24's Open Questions #1 already found that `AiSidebar` has no ancestor `RoomProvider` and recommended a one-directional callback-push-up (`onAiStatusChange`) rather than restructuring the provider tree — sufficient for a read-only feed. This spec both reads (render the message list) and writes (send a message), so the same boundary problem now needs a value flowing **up** (the message list, via a new `onChatMessagesChange` callback, mirroring `onAiStatusChange`) **and** a function flowing **down** (`sendMessage`, built inside `CanvasFlow` via `useMutation` and threaded down through `Canvas` → `WorkspaceShell` → `AiSidebar` → `AiArchitectTab` as a plain prop). Recommendation: extend the existing pattern symmetrically rather than introduce a new mechanism (e.g. a second Liveblocks provider scoped just to the sidebar) — no restructuring of the provider tree, consistent with spec 24's reasoning that doing so would be a much larger, riskier change for no benefit to a sidebar that has nothing to do with canvas rendering. This is the one structurally significant call in this brief, same category spec 24 flagged as its own Open Questions #1 — flagging for the Senior Developer to confirm.
4. **Some field details aren't fully pinned by the spec's own text.** The spec names exactly four fields (`sender`, `role`, `content`, `timestamp`) with no further detail on format or on whether a stable identifier is needed for list rendering. Recommendations: (a) `timestamp` as an epoch-ms `number` (`Date.now()`), a straightforward, sortable, easily-formatted choice — no precedent elsewhere in this codebase pins a different format; (b) `sender` as a display name resolved from the room's own already-resolved identity (Liveblocks `useSelf()`'s `info.name`, the same name `app/api/liveblocks-auth/route.ts` already computes from the caller's Clerk profile at session-auth time), not a raw Clerk user ID, for consistency with how names already surface elsewhere in this room (cursor badges, presence avatars); (c) a message `id` (not one of the spec's four named fields, but needed as a stable React list key once messages can arrive from multiple senders) — recommend adding it as a schema field anyway, since omitting it would force falling back to array index as a key for a live-appending list, which is a known React anti-pattern. All three are Dev-level implementation choices, not product decisions, and none change what's visibly rendered per the spec's own text.
5. **What "failure" means for a Storage-backed send is not specified.** The spec says "show a small error state if sending fails," but a Liveblocks Storage `LiveList` write via `useMutation` is a synchronous, locally-applied, optimistic operation — it doesn't hand back an HTTP-style promise that rejects on failure the way `PUT /api/projects/[projectId]/canvas` (spec 21) does. Recommendation: treat "failure" as (a) the outgoing message failing the Zod schema's own validation before the mutation is attempted, or (b) the mutation call itself throwing synchronously (wrapped in `try`/`catch`) — e.g. calling `sendMessage` while genuinely disconnected from the room. This is a Dev-level interpretation of an otherwise underspecified failure mode, not a literal requirement traceable to specific spec text beyond "show a small error state if sending fails."
6. **Whether `AiArchitectTab`'s existing local-only message state (spec 20) should be fully replaced or run alongside the new persisted feed.** Recommendation: full replacement. Spec 20's own docblock already flags that local `useState<ChatMessage[]>` array as "not a shape any other spec should import (spec 25 owns the real chat message schema in `types/tasks.ts`)" — signaling it was always meant to be superseded once persistence exists, not preserved as a second, parallel message list. Running both simultaneously would produce two independently-ordered message lists competing for the same rendered slot, which conflicts with this spec's own "render chat messages in order" requirement (there is exactly one order to preserve, not two).

### Out-of-scope callouts

- **AI-generated replies** — this spec's own explicit Scope Limit ("don't add AI-generated replies yet"). `role: "assistant"` stays schema-supported (reusing `ChatBubble`'s existing two-role styling) but is not reachable through anything this spec's code produces.
- **Triggering `/api/ai/design`, `/api/ai/spec`, or any Trigger.dev task from the chat UI** — this spec's own explicit Scope Limit ("don't trigger backend AI tasks"). That flow (submitting a prompt that actually triggers design generation) is spec 26's job (Design Agent Frontend), a separate concern from persisting/rendering chat messages.
- **`ai-status-feed`, AI presence (`thinking`/cursor), and the sidebar's generation status line/disabled-input/spinner behavior** — spec 23/24's already-delivered territory. This spec's own explicit Scope Limit ("keep it separate from `ai-status-feed`," "don't mix chat messages with status messages") means no shared payload type, no shared subscription, and no reuse of `hooks/use-ai-status-feed.ts` or its schema.
- **Any new realtime mechanism outside Liveblocks** (WebSocket, Server-Sent Events, a polling loop) — this spec's own explicit Scope Limit ("don't create a parallel realtime system outside Liveblocks").
- **Message history pruning/pagination, chat search, message editing or deletion, read receipts, typing indicators** — none of this appears anywhere in the spec's text ("render chat messages in order," "allow users... to send messages," "validate... before rendering" is the full extent of what's asked); adding any of it would be inventing product behavior beyond what the spec states.
- **Billing/subscriptions, enterprise permission tiers, versioned spec history, production object storage migration, mobile apps** — `project-overview.md`'s Out of Scope wall; none of this spec's work touches any of them.

Brief ready for Senior Developer at `context/spec-status/25-sidebar-chat-feed.md`.

## Dev Notes

### Files added/changed

- `package.json`/`package-lock.json` (modified) — added `zod@^4.4.3` as a real production dependency (confirmed genuinely missing beforehand), per the brief's Open Questions #1 recommendation. This is the first spec in this codebase to use Zod.
- `types/tasks.ts` (modified, additive) — `AiChatMessageSchema` (`z.object({ id, sender, role, content, timestamp })`, all string fields `min(1)`, `role: z.enum(["user", "assistant"])`, `timestamp: z.number().finite()`) and its inferred `AiChatMessage` type (`z.infer<typeof AiChatMessageSchema>`). Also exports `SendChatMessage = (content: string) => void`, the function-shape type threaded down through `Canvas` → `WorkspaceShell` → `AiSidebar` → `AiArchitectTab`. `AiStatusStage`/`AiStatusMessage`/`isAiStatusMessage` (spec 24) are untouched — no shared type, no cross-import between the two feeds (acceptance criterion 6).
- `hooks/use-ai-chat-feed.ts` (new, per `architecture-context.md`'s Hooks Convention) — `useAiChatFeed()`: reads the room's `ai-chat` Storage `LiveList` via `useStorage((root) => root.messages)`, validates every entry through `AiChatMessageSchema.safeParse` inside a `useMemo` keyed on the raw array reference (Liveblocks Storage reads are immutable/structurally-shared, so this only re-runs when the list genuinely changes, not on every unrelated room update — the `performant-storage-with-selectors` pattern from this repo's bundled Liveblocks skill), dropping invalid entries rather than rendering or crashing on them. Builds `sendMessage` via `useCallback` wrapping a `useMutation`-built `pushMessage`: constructs the outgoing message (`id` via a `Date.now()` + random-suffix generator, `sender` from `useSelf().info.name`, `role: "user"`, `timestamp: Date.now()`), validates it against the same schema, throws synchronously *before* ever calling the mutation if invalid, and otherwise pushes the validated message onto the `LiveList`. Must be called from inside the room's `RoomProvider` boundary (same constraint spec 24's `useAiStatusFeed` already documents).
- `liveblocks.config.ts` (modified) — added a `Storage` type (`{ messages: LiveList<AiChatMessage> }`) to the existing global `Liveblocks` interface augmentation, imported from `types/tasks.ts`, matching the file's established `Presence`/`UserMeta` convention. Per the brief's Open Questions #2 recommendation.
- `components/editor/canvas.tsx` (modified) — `RoomProvider` gains `initialStorage={{ messages: new LiveList([]) }}` (new `LiveList` import from `@liveblocks/client`). `CanvasProps`/`CanvasFlow`'s props gain `onChatMessagesChange`/`onSendChatMessageChange`. `CanvasFlow` calls `useAiChatFeed()` and pushes `messages` up via `onChatMessagesChange` and `sendMessage` down via `onSendChatMessageChange`, both via the same effect-based callback-push-up shape `onAiStatusChange`/`onSaveStatusChange` already use.
- `components/editor/workspace-shell.tsx` (modified) — new `chatMessages`/`sendChatMessage` state, mirroring `aiStatus`. `onChatMessagesChange={setChatMessages}` wired into `<Canvas>`; `onSendChatMessageChange={(sendMessage) => setSendChatMessage(() => sendMessage)}` (wrapped in an updater-function form — storing a plain function value directly in `useState`'s setter would be misread by React as an updater function). `sendChatMessage` initializes to a `chatNotReadyYet()` default that throws (not a silent no-op), so a send attempted before the room connects surfaces through `AiArchitectTab`'s own try/catch as a genuine failure (input preserved, error shown) rather than quietly discarding the message.
- `components/editor/ai-sidebar.tsx` (modified) — new required `chatMessages`/`sendChatMessage` props, forwarded straight through to `<AiArchitectTab>`. No new state owned here, same posture as `aiStatus`.
- `components/editor/ai-architect-tab.tsx` (modified — full rewrite of this component's message-state ownership, per the brief's explicit authorization): removed the local-only `useState<ChatMessage[]>` array and its `handleSubmit` push (spec 20); the message list now renders the real `chatMessages` prop through `ChatBubble` (extended to show `sender` and a formatted `timestamp` above the content, `text-copy-muted`, locale pinned to `"en-US"` for a deterministic format shape across test environments). Submitting calls the passed-down `sendMessage(trimmed)` inside a `try`/`catch`: on success, clears the input and any stale error; on failure (schema validation failing before the mutation, or the mutation itself throwing), preserves the input and shows a small inline error indicator (`AlertCircle` + `text-state-error`, matching `SaveStatusIndicator`'s existing error convention) — cleared automatically once the user edits the input again (a Dev-level UX call, not pinned by the brief's text). Starter-prompt-chip fill-not-submit behavior, Enter/Shift+Enter handling, and the spec-24 `aiStatus` status line/disabled/spinner logic are untouched.
- Tests: `types/tasks.test.ts` (extended — `AiChatMessageSchema` accept/reject cases for all four named fields plus the `id`/structural-independence-from-`ai-status-feed` cases), `hooks/use-ai-chat-feed.test.ts` (new — subscribes via the real `useStorage`, filters invalid Storage entries including a wholly foreign `ai-status-feed`-shaped payload, `sendMessage` resolves sender/role/id/timestamp correctly and validates before mutating, throws without mutating on invalid content), `components/editor/ai-architect-tab.test.tsx` (extended/rewritten — renders persisted messages with sender/timestamp/content in order, calls `sendMessage` with trimmed content, clears input on success, preserves input and shows the error indicator on failure, clears a stale error on edit, defaults to a throwing `sendMessage` when the prop is absent), `components/editor/canvas.test.tsx` (extended — `initialStorage`'s `messages` `LiveList` is present and empty, `useAiChatFeed` called and its `messages`/`sendMessage` pushed up/down correctly), `components/editor/workspace-shell.test.tsx` (extended — `chatMessages`/`sendChatMessage` threading through to `AiSidebar`, including the "not ready yet" default-throws-and-shows-error case), `components/editor/ai-sidebar.test.tsx` (extended — every existing render call updated to pass the now-required `chatMessages`/`sendChatMessage` props; new tests confirming both forward straight through to `AiArchitectTab`).
- No changes to `trigger/design-agent.ts`, `lib/design-agent-ai.ts`, `lib/design-agent-room.ts`, `app/api/ai/design*`, `hooks/use-ai-status-feed.ts`, or `components/editor/live-cursors.tsx` — confirmed via `git status`/`git diff --stat` that none of those files appear in this branch's diff, matching this spec's own explicit "no touch" list.

### Skills used

- `liveblocks-best-practices` (`.claude/skills/liveblocks-best-practices`) — read `references/create-custom-realtime-multiplayer.md` and `references/type-liveblocks-correctly.md` for the `Storage`/`LiveList` type-augmentation convention (confirmed `liveblocks.config.ts`'s existing `Presence`/`UserMeta` pattern extends the same way for `Storage`), `references/performant-storage-with-selectors.md` for the `useStorage` selector/memoization pattern `useAiChatFeed` uses to avoid re-validating on every unrelated room update, and `references/exhaustive-deps-with-usemutation.md` to confirm this repo's `eslint.config.mjs` has no `additionalHooks` configuration for `useMutation` (so its own deps array isn't lint-checked — mitigated here by keeping the `useMutation` callback's own deps array at `[]`, since it closes over nothing but its own parameters, and doing sender resolution/validation *outside* the mutation in a separately-`useCallback`'d `sendMessage` with an explicit `[self, pushMessage]` deps array instead). Also verified `useMutation`/`useStorage`/`useSelf`'s real exported signatures directly against the installed package (`node_modules/@liveblocks/react/dist/room-DUr5FIYB.d.ts` and `suspense.d.ts`'s export list) rather than assuming them from training data, per this repo's "read the installed SDK, not training data" precedent (specs 18/22/23/24).

### Key decisions

1. **Open Questions #1 (install Zod) — followed the brief's recommendation as-is.** `zod@^4.4.3` installed as a real production dependency; `AiChatMessageSchema` is a genuine `z.object(...)` schema, not a hand-rolled guard relabeled as one. `isAiStatusMessage` (spec 24) is left exactly as-is — no retroactive migration to Zod, since that feed's own spec text never named a validation library and migrating it wasn't in scope here.
2. **Open Questions #2 (`initialStorage`/`Storage` type on the shared `RoomProvider`) — added as recommended.** `initialStorage={{ messages: new LiveList([]) }}` and a matching `Storage` type in `liveblocks.config.ts`. Verified via the full test suite (all pre-existing `canvas.test.tsx` tests pass unmodified) that this is genuinely additive — `useLiveblocksFlow`'s own node/edge management is untouched, since it doesn't read the declared `Storage` root shape at all.
3. **Open Questions #3 (bidirectional wiring through the provider boundary) — extended the existing callback-push-up pattern symmetrically, no new provider/context.** `onChatMessagesChange` mirrors `onAiStatusChange` exactly (state flows up); `onSendChatMessageChange` is the new "function flows down" counterpart — `CanvasFlow` pushes `useAiChatFeed()`'s `sendMessage` up to `WorkspaceShell` via an effect (same shape as the messages push), and `WorkspaceShell` stores it in `useState`, wrapped in an updater-function form (`setSendChatMessage(() => sendMessage)`) to avoid React's setState-treats-a-function-as-an-updater ambiguity. No second `RoomProvider`/context was introduced.
4. **Open Questions #4(a) (`timestamp` as epoch-ms `Date.now()`) — followed as recommended.** Sortable, easily formatted, no existing precedent pinning a different format.
5. **Open Questions #4(b) (`sender` from `useSelf().info.name`, not a raw Clerk ID) — followed as recommended.** `useAiChatFeed`'s `sendMessage` reads `self.info.name` (the suspense-mode `useSelf()` return is non-null, since `CanvasFlow` is always inside `ClientSideSuspense`), consistent with how names already surface in cursor badges/presence avatars.
6. **Open Questions #4(c) (add a stable `id` field) — added as recommended.** `AiChatMessageSchema` includes `id: z.string().min(1)`, generated client-side (`Date.now()` + a random base36 suffix, the same "good enough, not cryptographic" bar `lib/canvas-shapes.ts#generateNodeId` already sets) — used as `ChatBubble`'s React list key instead of falling back to array index.
7. **Open Questions #5 (what "failure" means for a Storage-backed send) — implemented both interpretations the brief recommended.** `sendMessage` throws synchronously (a) before ever calling the mutation if the outgoing message fails `AiChatMessageSchema`'s own validation, and (b) if the underlying Storage mutation itself throws. `AiArchitectTab`'s `handleSubmit` wraps the call in `try`/`catch`: success clears the input, failure preserves it and shows the inline error indicator. `WorkspaceShell`'s default `sendChatMessage` (before `CanvasFlow`'s real one arrives) also throws rather than silently no-opping, so a send attempted in that narrow window is a genuine, visible failure, not a discarded message.
8. **Open Questions #6 (full replacement of spec 20's local message state) — implemented as recommended, no parallel list.** `AiArchitectTab`'s `useState<ChatMessage[]>` and its `handleSubmit` push are gone entirely; the rendered list is exclusively the persisted `chatMessages` prop. Confirmed via the full rewrite of `ai-architect-tab.test.tsx` that no test still asserts against the old local-echo behavior.
9. **Error-indicator clearing on input edit** — a Dev-level UX call not pinned by the brief's text ("preserves the input... shows a small error indicator" says nothing about when the indicator itself should disappear before a retry). Chose to clear it on the very next `onChange` rather than leaving it showing indefinitely next to text the user has already started changing; a fresh failed retry re-shows it.
10. **`ChatBubble`'s sender/timestamp styling** — both rendered in a shared `text-copy-muted` sub-line above the content, for both `user`/`assistant` roles, rather than tinting the metadata line to match each role's own accent — a Dev-level styling choice (the spec's own text only asks to "show sender, timestamp, and message content," not to pin their relative visual weight).

### Test coverage

- `types/tasks.test.ts` (extended) — `AiChatMessageSchema` accepts a well-formed `user`/`assistant` message; rejects a missing/empty `id`, missing/empty `sender`, missing/unrecognized `role`, missing/empty `content`, missing/non-number/non-finite `timestamp`, a wholly foreign payload (string/null/undefined/array), and an `ai-status-feed`-shaped payload (acceptance criterion 6, structural independence).
- `hooks/use-ai-chat-feed.test.ts` (new, mocking `@liveblocks/react/suspense#useStorage`/`useSelf`/`useMutation` via `vi.hoisted`) — subscribes via the real `useStorage` mechanism and returns every valid message in order; filters out an invalid role, a wholly foreign `ai-status-feed`-shaped entry, and an empty-content entry, keeping only the valid one; returns an empty list without crashing when Storage is empty; `sendMessage` resolves `sender` from `useSelf().info.name` with `role: "user"`, a generated `id`/`timestamp`, and pushes the validated message; throws before mutating (and never calls `push`) when the outgoing content fails validation (empty string); generates a different `id` per call.
- `components/editor/ai-architect-tab.test.tsx` (extended/rewritten) — starter-chip fill-not-submit and Shift+Enter-inserts-newline behavior preserved (now asserted via a `sendMessage` spy not being called, since there's no more local echo to check); renders every persisted message in order with sender/timestamp/content visible; submits via Enter and the Send button, calling `sendMessage` with trimmed content and clearing the input on success; preserves the input and shows the error indicator when `sendMessage` throws; clears a stale error indicator on the next edit; defaults to a throwing `sendMessage` (and the resulting failure UI) when no prop is wired up. All pre-existing spec-24 `aiStatus` tests (status line, disabled state, spinner) pass unmodified. `ChatBubble` tests extended for sender/timestamp/content rendering per role, with the exact clock value left unasserted (locale/timezone-dependent) in favor of a "something time-like rendered" regex check.
- `components/editor/canvas.test.tsx` (extended) — `RoomProvider`'s `initialStorage.messages` is a `LiveList` with `length === 0`; `useAiChatFeed` is called from inside `CanvasFlow`; its `messages` push up via `onChatMessagesChange` (both the empty default and an overridden list) and its `sendMessage` push down via `onSendChatMessageChange` are both verified.
- `components/editor/workspace-shell.test.tsx` (extended) — `Canvas` mock extended with two more "simulate" buttons standing in for `CanvasFlow`'s real `useAiChatFeed()` push (same pattern the existing "simulate save"/"simulate ai status" buttons already established); confirms `chatMessages` reaches the rendered `AiArchitectTab`, confirms a pushed-down `sendMessage` is genuinely usable (successful send clears input, no error), and confirms the *default* `sendChatMessage` (before any push happens) fails visibly rather than silently.
- `components/editor/ai-sidebar.test.tsx` (extended) — every pre-existing `render(<AiSidebar ... />)` call updated to pass the now-required `chatMessages={[]}`/`sendChatMessage={vi.fn()}` props; new tests confirm both forward straight through to `AiArchitectTab`'s rendered output/behavior.
- Full suite: 494/494 tests passing across 54 files (up from 459/53 at the end of spec 24), via `npx vitest run --no-file-parallelism`.
- `npx tsc --noEmit` — clean. `npx eslint .` — clean on every file this diff touches (confirmed via a targeted run scoped to exactly the 13 files this diff added/changed, zero errors/warnings); the full-repo run's reported errors/warnings are entirely pre-existing and confined to generated `.trigger/tmp/` build artifacts and a third-party `.agents/skills/` template, neither touched by this diff (same category of noise specs 23/24's Dev Notes already flagged). `npx next build` — passes; same route list as before, no new build-time dependency introduced (Zod needs no environment variable/runtime credential).

### Known limitations / deferrals

- No live browser/multiplayer verification was possible in this pipeline (consistent with every prior canvas/presence spec) — the persisted chat feed, the send/clear/error-indicator flow, and the "a participant joining mid-conversation sees prior messages" replay guarantee are verified via mocked unit tests only, not against a real Liveblocks room. Recommend a human smoke test: open two tabs on the same project, send a message from one, confirm the *other* tab's sidebar renders it (room-wide, not local-only), then open a *third* tab afterward and confirm it sees the full prior history (the actual reason `architecture-context.md` pins `ai-chat` to a Storage `LiveList` rather than `broadcastEvent`).
- Per the brief's own Scope Limits, no AI-generated reply, no `/api/ai/*`/Trigger.dev triggering, and no touching of `ai-status-feed`/AI presence code paths — confirmed via `git status`/`git diff --stat` that `trigger/design-agent.ts`, `lib/design-agent-ai.ts`, `lib/design-agent-room.ts`, `app/api/ai/design*`, `hooks/use-ai-status-feed.ts`, and `components/editor/live-cursors.tsx` are all genuinely untouched by this diff.
- The error-indicator auto-clear-on-edit behavior (Key Decision #9) is a Dev-level UX interpretation, not literal spec text — flagging for QA/PO awareness in case a stricter "only clears on next successful send" reading is preferred.

Implementation ready for QA at `context/spec-status/25-sidebar-chat-feed.md`.
