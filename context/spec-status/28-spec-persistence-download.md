Source spec: `context/feature-specs/28-spec-persistence-download.md`

## Analyst Brief

### Scope statement

Add a `ProjectSpec` Prisma metadata model, persist a generated spec's Markdown content to Vercel Blob (following the same metadata-in-Postgres/content-in-Blob split `lib/canvas-blob.ts` already established for canvas snapshots), and add one authenticated, access-checked `GET` route that returns a persisted spec as a downloadable Markdown file. This is the backend persistence/download half spec 27 deliberately deferred — no frontend/UI logic, and no spec editor/viewer UI (that is spec 29's job, per `components/editor/specs-tab.tsx`'s own docblock and `context/feature-specs/29-spec-ui-integration.md`'s own text).

### Concrete deliverables

- `prisma/schema.prisma` (modified) — new `ProjectSpec` model per the raw spec text's own field list (`id`, `projectId` relation, `filePath`, `createdAt`), with a `@relation` to `Project` (`onDelete: Cascade`, mirroring `TaskRun`'s existing precedent) and a `specs ProjectSpec[]` back-relation added to `Project`. Requires a real migration against the provisioned Postgres database (same as spec 22's `TaskRun` addition). No `userId` field — access is derived from the project relationship via `getProjectAccess`, not per-spec ownership (the raw spec text's own field list has no `userId`, and nothing in the spec asks for one).
- `lib/spec-blob.ts` (new) — a Blob upload/fetch helper module mirroring `lib/canvas-blob.ts`'s exact shape (`requireBlobToken`, lazy per-call token check, no eager client instantiation so `next build`'s page-data collection stays safe): `uploadSpecMarkdown(projectId, specId, markdown)` writing to `specs/{projectId}/{specId}.md` (the exact pathname `architecture-context.md`'s Storage Model already names) and `fetchSpecMarkdown(blobUrl)` for the download route to read content server-side. **Must use `access: "private"`**, matching `lib/canvas-blob.ts`'s just-fixed, live-confirmed store configuration (a `"public"` `put`/`get` call is rejected outright by the provisioned store) — this is not optional or a fresh decision, it is the already-established, already-verified convention for this exact Blob store.
- The persistence step itself ("after a spec is generated: upload to Blob, store the path, link to the project") — concrete file TBD by the Senior Developer per Open Questions #1 below, but expected to live in either `trigger/generate-spec.ts` (modified) or a new dedicated route, not inline in the download route.
- `app/api/projects/[projectId]/specs/[specId]/download/route.ts` (new) — `GET`: `getProjectAccess(projectId)` (owner-or-collaborator, matching `app/api/projects/[projectId]/canvas/route.ts`'s existing gate rather than an owner-only one, since collaborators who can view/edit the canvas and trigger generation should also be able to download the result) → `prisma.projectSpec.findUnique({ where: { id: specId } })` → verify `spec.projectId === projectId` (404 if the spec exists but belongs to a different project — the raw spec text's own explicit "verify the spec belongs to that project" instruction) → `fetchSpecMarkdown(spec.filePath)` → return the Markdown as a downloadable attachment (`Content-Type: text/markdown; charset=utf-8`, `Content-Disposition: attachment; filename="..."` — exact filename convention is Open Questions #4) — never the raw Blob URL, mirroring the canvas `GET` route's "content only, never the URL" convention.
- Tests: `lib/spec-blob.test.ts` (new), the download route's own `route.test.ts` (new), and a test file for wherever the persistence step lands (new or extended, per Open Questions #1) — following this repo's established `vi.mock`/`vi.hoisted` conventions for Prisma, Clerk `auth()`, and the Blob/Trigger.dev SDK boundaries.
- No `components/*` file changes anywhere in this diff — the raw spec text's own explicit Scope Limit ("do not add frontend or UI logic"). `components/editor/specs-tab.tsx`'s inert "Generate Spec" button and static demo card stay exactly as spec 20 left them.
- No change to `lib/canvas-blob.ts`, `app/api/projects/[projectId]/canvas/route.ts`, or `hooks/use-canvas-autosave.ts` — the raw spec text's own explicit Scope Limit ("do not modify existing canvas persistence").

### Acceptance criteria

1. A `ProjectSpec` Prisma model exists with `id`, a `projectId` relation to `Project` (cascade-deleted with the project, matching `TaskRun`'s precedent), `filePath`, and `createdAt`. No spec Markdown content is stored as a Prisma column anywhere.
2. Once a spec's Markdown content is available, it is uploaded to Vercel Blob at `specs/{projectId}/{specId}.md` using `access: "private"`, and a `ProjectSpec` row is created (or updated) with the resulting Blob URL in `filePath`, linked to the correct `projectId`.
3. `GET /api/projects/[projectId]/specs/[specId]/download` returns 401 for an unauthenticated caller, and 404/403 (via `getProjectAccess(projectId)`) for a missing or non-member project, before any Prisma read or Blob fetch happens.
4. The same route returns 404 when `specId` does not exist, or exists but belongs to a different `projectId` than the one in the URL.
5. Given a valid authenticated owner-or-collaborator request and a real, existing spec belonging to that project, the route fetches the Markdown content from Blob server-side and returns it as a downloadable file (`Content-Type: text/markdown`, `Content-Disposition: attachment`) — never the raw Blob URL in the response body or headers.
6. A genuine upstream Blob failure (network/auth error, not "not found") surfaces as a 500, not a silently-empty or miscategorized 404 — mirroring `fetchCanvasSnapshot`'s existing distinction between "nothing there" and "a real failure happened."
7. No spec content is ever stored directly in Postgres — `ProjectSpec` holds only metadata (`filePath` + relations/timestamps), matching `code-standards.md`'s "Do not store large generated content directly in the database."
8. No Blob URL is ever exposed to an unauthenticated or non-member caller — every path to a spec's content goes through the access-checked download route.
9. `components/editor/specs-tab.tsx` and every other file under `components/` are unmodified anywhere in this diff.
10. `lib/canvas-blob.ts` and `app/api/projects/[projectId]/canvas/route.ts` are unmodified anywhere in this diff.
11. `npx tsc --noEmit`, `npx eslint .`, `npx vitest run`, and `npx next build` all pass, including a real Prisma migration for the new `ProjectSpec` model applied against the environment's provisioned database.

### Dependencies

- **Spec 27 (Spec Generation Flow)** — `trigger/generate-spec.ts`'s `runGenerateSpec`/`generateSpecTask`, which returns `{ roomId, projectId, markdown }` as task output with zero persistence, is this spec's direct input. **Complete** (QA PASS, Product Owner PASS, PR #23 open against `main`, not yet merged — see Open Questions #1 for why this matters for where the persistence step lands).
- **Spec 21 (Canvas Autosave)** — `lib/canvas-blob.ts`'s exact upload/fetch/lazy-token pattern this spec's `lib/spec-blob.ts` mirrors, and its just-fixed, live-confirmed `access: "private"` store configuration. **Complete**, and the private-access fix is already merged into the codebase (confirmed by reading the current `lib/canvas-blob.ts` directly) — this spec does not need to rediscover that the store is private, only follow the same convention from the start.
- **Spec 22 (Design Agent API)** — `TaskRun`'s Prisma model as the precedent this spec's `ProjectSpec` model (relation shape, cascade delete, index conventions) should follow. **Complete.**
- **`lib/project-access.ts#getProjectAccess`** — the owner-or-collaborator gate this spec's download route reuses unmodified. **Complete, pre-existing infrastructure.**
- **Spec 20 (AI Sidebar Shell)** — `components/editor/specs-tab.tsx`'s inert "Generate Spec" button/demo card, deliberately left untouched by this spec (spec 29's job). **Complete, not modified by this spec.**
- **Spec 29 (Spec UI Integration, not yet started)** — the raw text of `context/feature-specs/29-spec-ui-integration.md` explicitly assumes "the existing ProjectSpec API" for fetching a spec list, and a separate "existing endpoint" for fetching content for a preview modal (distinct from the download endpoint, which triggers a browser file save rather than returning content for in-app rendering). This spec's own raw text does **not** name a list route as one of its three numbered implementation items — see Open Questions #3 for how this brief resolves that tension.

### Open questions

1. **Where does the persistence step (`ProjectSpec` create/update + Blob upload) actually live — inside `trigger/generate-spec.ts` itself, or a separate route?** The raw spec text's step 2 ("Save generated spec — After a spec is generated: upload…, store…, link…") describes what must happen but not what triggers it; unlike step 3 (the download route), it names no route path. Two real candidates: (a) modify `trigger/generate-spec.ts` so `runGenerateSpec` persists automatically right after `generateSpecMarkdown` succeeds, before the task returns; or (b) add a new, separate route (e.g. `POST /api/projects/[projectId]/specs`) that something — most plausibly spec 29's future frontend, reading the completed run's Markdown via `useRealtimeRun`'s output — calls afterward to persist it. **Recommendation: (a).** `architecture-context.md`'s own "Spec Generation" section already states the task's output is "Markdown technical spec **saved to the filesystem and linked to the project in the database**" — i.e., persistence is described as part of the task's own execution, not a separate client-triggered step. Prisma/Blob calls from inside a Trigger.dev task have no precedent yet in this codebase (`trigger/design-agent.ts` only touches Liveblocks, not Prisma), but nothing in `architecture-context.md`'s invariants restricts background tasks from writing to Postgres/Blob — Invariant 1 restricts *request handlers*, not background tasks. This also avoids inventing a new, spec-text-unnamed persistence route. Flagged clearly as the single largest interpretive gap in this spec's raw text — confirm before implementing, since it changes which file(s) are the primary deliverable.
2. **How is the Blob pathname's `specId` known before the `ProjectSpec` row exists?** `specs/{projectId}/{specId}.md` needs an ID, but Prisma's `@default(cuid())` only generates one at insert time. Recommendation: a two-write pattern — `prisma.projectSpec.create({ data: { projectId, filePath: "" } })` first (or a temporary placeholder value) to obtain the generated `id`, then `uploadSpecMarkdown(projectId, spec.id, markdown)`, then `prisma.projectSpec.update({ where: { id: spec.id }, data: { filePath: blobUrl } })`. This avoids adding a new ID-generation library dependency purely to precompute a cuid client-side. A Senior Developer who prefers a single-write approach (e.g., a library-generated ID passed explicitly as `id` on create) should confirm the tradeoff first — this is a small mechanical choice, not a product-behavior one.
3. **Whether this spec should also add a minimal `GET /api/projects/[projectId]/specs` list route**, even though the raw spec text's three numbered implementation items and "Check When Done" checklist name only the model, the save step, and the download route. **Recommendation: yes, add it**, for three converging reasons: (a) `project-overview.md`'s Features section states "Users can view and download generated specs" as part of Spec Generation — "view" implies a list is discoverable somewhere; (b) the download route's own "verify the spec belongs to that project" requirement presupposes a client already knows a real `specId`, which has to come from somewhere; (c) spec 29's raw text explicitly assumes "the existing ProjectSpec API" for fetching a list — without it, spec 29 is blocked on day one. This is flagged as a recommendation, not a hard requirement, because it genuinely goes beyond this spec's own literal checklist — if the Senior Developer disagrees and omits it, that should be called out explicitly in Dev Notes so the gap is visible to spec 29's own analyst pass rather than silently discovered mid-implementation. If added: metadata-only (`id`, `filePath` or a derived filename, `createdAt`), same `getProjectAccess(projectId)` gate, ordered by `createdAt desc`.
4. **Exact `Content-Disposition` filename convention.** Nothing in the raw spec text or `ui-context.md` pins one. Recommendation: `spec-{specId}.md` — stable, collision-free, no dependency on project name sanitization. A Senior Developer preference for a project-name-derived filename is a reasonable alternative but should be a documented choice, not left implicit.
5. **Whether multiple `ProjectSpec` rows per project are expected, or whether generation should overwrite a single record** (the way `Project.canvasJsonPath` holds exactly one snapshot). The raw spec text's own model has no unique constraint on `projectId`, and spec 29's raw text explicitly describes "a list of specs" (plural, each with its own `createdAt`) — not a single latest record. **Recommendation: allow multiple rows, one per generation run, no overwrite.** This is not the same as `project-overview.md`'s Out of Scope "versioned spec history and review workflows" — there is no version numbering, diffing, or review/approval flow here, just multiple independently generated artifacts a user can individually download, which the Features section's own "Specs are persisted as files and linked to the project" text already supports.

### Out-of-scope callouts

- **Spec list UI, preview modal, and wiring the "Generate Spec" button** (`components/editor/specs-tab.tsx`) — explicitly spec 29's job per that file's own docblock and `context/feature-specs/29-spec-ui-integration.md`. This spec adds no `components/*` change.
- **Storing spec Markdown content in Prisma** — the raw spec text's own explicit Scope Limit; `ProjectSpec` is metadata-only, content lives in Blob.
- **Modifying existing canvas persistence** (`lib/canvas-blob.ts`, `app/api/projects/[projectId]/canvas/route.ts`, `hooks/use-canvas-autosave.ts`) — the raw spec text's own explicit Scope Limit.
- **Exposing Blob URLs without access checks** — the raw spec text's own explicit Scope Limit; the download route always fetches content server-side and returns the content, never the URL.
- **Versioned spec history, diffing, or review/approval workflows** — `project-overview.md`'s Out of Scope wall. Allowing multiple independently-downloadable generated specs (Open Questions #5) is not the same thing and does not cross this line.
- **Rate limiting on spec generation or download** — already logged in `progress-tracker.md`'s "Deferred — Production Hardening" section, which names `/api/ai/spec`; not this spec's job.
- **Billing/subscriptions, enterprise permission tiers, production object storage migration, mobile apps** — `project-overview.md`'s Out of Scope wall; none of this spec's work touches any of them.

Brief ready for Senior Developer at `context/spec-status/28-spec-persistence-download.md`.

## Dev Notes

### Files added

- `lib/spec-blob.ts` (new) — Blob upload/fetch helpers mirroring `lib/canvas-blob.ts`'s exact shape: `requireBlobToken()` (lazy, per-call token check — no eager client instantiation, so a missing `BLOB_READ_WRITE_TOKEN` doesn't break `next build`'s page-data collection), `specBlobPathname(projectId, specId)` → `specs/{projectId}/{specId}.md`, `uploadSpecMarkdown(projectId, specId, markdown)`, and `fetchSpecMarkdown(blobUrl)`. `SPEC_BLOB_ACCESS = "private" as const`, matching `lib/canvas-blob.ts`'s just-fixed, live-confirmed store configuration exactly — this was explicitly called out as not a fresh decision, and it wasn't treated as one. `fetchSpecMarkdown` returns `null` only for "nothing there" (not-found / non-200) and lets a genuine upstream error (network/auth failure) propagate, mirroring `fetchCanvasSnapshot`'s existing distinction so the download route can tell a real outage from "no content" (acceptance criterion 6).
- `app/api/projects/[projectId]/specs/route.ts` (new) — `GET`: `getProjectAccess(projectId)` (owner-or-collaborator) → `prisma.projectSpec.findMany({ where: { projectId }, orderBy: { createdAt: "desc" } })` → returns `{ specs: [{ id, filename, createdAt }] }`. Resolves Open Questions #3 toward "yes, add it" — not named in the raw spec text's three numbered implementation items, added because the download route's own "verify the spec belongs to that project" check presupposes a client already knows a real `specId`, and spec 29's raw text explicitly assumes "the existing ProjectSpec API" for a list. Returns a derived `filename` (`spec-{id}.md`), **not** `ProjectSpec.filePath` — the brief's own Open Questions #3 text offered either "filePath or a derived filename" as acceptable; the derived filename was chosen so this route never puts a raw Blob URL in a response body, consistent with every other Blob-backed route in this codebase (`canvas`, and this spec's own download route) and with acceptance criterion 8's "no Blob URL is ever exposed... every path to a spec's content goes through the access-checked download route" spirit, even though criterion 8's literal text only names "unauthenticated or non-member caller."
- `app/api/projects/[projectId]/specs/[specId]/download/route.ts` (new) — `GET`: `getProjectAccess(projectId)` → `prisma.projectSpec.findUnique({ where: { id: specId } })` → verifies `spec.projectId === projectId` (404 if the spec exists but belongs to a different project, the raw spec text's own explicit instruction) → `fetchSpecMarkdown(spec.filePath)` → returns the Markdown as `Content-Type: text/markdown; charset=utf-8` / `Content-Disposition: attachment; filename="spec-{specId}.md"` (Open Questions #4's recommended convention, adopted as-is). Failure precedence: 401 → 404/403 (`getProjectAccess`) → 404 unknown/foreign `specId` → 404 missing Blob content → 500 genuine upstream Blob failure.
- Tests: `lib/spec-blob.test.ts`, `app/api/projects/[projectId]/specs/route.test.ts`, `app/api/projects/[projectId]/specs/[specId]/download/route.test.ts` (all new).

### Files modified

- `prisma/schema.prisma` — new `ProjectSpec` model (`id`, `projectId` relation to `Project` with `onDelete: Cascade` mirroring `TaskRun`'s precedent, `filePath`, `createdAt`, `@@index([projectId, createdAt])`). No `userId` field — access is derived from the project relationship via `getProjectAccess`, not per-spec ownership, per the brief's explicit instruction. No unique constraint on `projectId` — multiple independently-downloadable `ProjectSpec` rows per project are expected (Open Questions #5, resolved toward "allow multiple rows, one per generation run, no overwrite"). `Project` gains a `specs ProjectSpec[]` back-relation. A real migration (`prisma/migrations/20260822130551_add_project_spec/`) was created and applied via `prisma migrate dev --name add_project_spec` against the environment's provisioned Postgres database (confirmed by reading the generated `migration.sql` directly — a plain `CREATE TABLE`/`CREATE INDEX`/`ADD CONSTRAINT`, no destructive operation).
- `trigger/generate-spec.ts` — resolves Open Questions #1 toward option (a): persistence lives inside `runGenerateSpec` itself, not a separate client-triggered route. After `generateSpecMarkdown` succeeds, a new `persistGeneratedSpec(projectId, markdown)` helper implements the two-write pattern Open Questions #2 recommended: `prisma.projectSpec.create({ data: { projectId, filePath: "" } })` to obtain a generated `id`, then `uploadSpecMarkdown(projectId, spec.id, markdown)`, then `prisma.projectSpec.update({ where: { id: spec.id }, data: { filePath } })`. If the Blob upload or the follow-up update throws, the placeholder row is deleted (`prisma.projectSpec.delete`, wrapped in its own `.catch` so a secondary cleanup failure can't mask the original error, itself logged separately) before rethrowing — an addition beyond the brief's literal two-write recommendation, made because leaving an orphaned, empty-`filePath` row behind would surface a spec in the list/download routes that can never actually be fetched, a silent-failure mode `code-standards.md`'s "write real error handling" rule argues against. `GenerateSpecResult` gained a `specId` field (the persisted `ProjectSpec.id`) alongside the pre-existing `roomId`/`projectId`/`markdown` fields — not asked for explicitly by the brief's acceptance criteria, but a natural, minimal addition: it lets a future frontend consuming this task's output via `useRealtimeRun` (spec 29+) locate the persisted spec without a separate lookup, and costs nothing extra since the id is already in hand at that point in the function. A second `"processing"` status ("Ghost AI is saving the generated spec…") is published via the pre-existing `metadata.set` mechanism between Gemini generation and the final `"complete"` status, so the persistence step (which does real I/O and can itself fail) isn't silently folded into the prior "drafting" status.
- `trigger/generate-spec.test.ts` — added `@/lib/prisma` and `@/lib/spec-blob` mocks (`vi.hoisted`), updated `beforeEach` with default resolved values for `projectSpec.create`/`update`/`delete` and `uploadSpecMarkdown`, updated existing assertions for the new `specId` field and the extra `"processing"` status, and added a new "runGenerateSpec — persistence" describe block (5 new tests, see Test coverage below).

### Skills used

- `prisma-cli` — consulted before running `prisma migrate dev --name add_project_spec` (confirmed this is the correct additive-migration command, distinct from any destructive `migrate reset`/`db push --force-reset` command that would need explicit human consent per the skill's "AI Safety Checkpoint" section — not applicable here, since adding a new table is non-destructive).
- `prisma-client-api` was not separately consulted as a fresh reference — `prisma.projectSpec.create`/`update`/`delete`/`findUnique`/`findMany` are all byte-for-byte the same call shapes `TaskRun`'s pre-existing Prisma usage (spec 22/27) already established in this codebase; no new query pattern was needed.

### Key decisions (brief's Open Questions)

1. **Where persistence lives** — inside `trigger/generate-spec.ts` (option (a)), per the brief's recommendation. Confirmed via direct reading of `architecture-context.md`'s "Spec Generation" section (already described the task's output as "saved to the filesystem and linked to the project") that this reading was correct before implementing.
2. **Blob pathname's `specId` before the row exists** — the two-write pattern (create placeholder → upload → update), per the brief's recommendation, with an added best-effort cleanup-on-failure step (see "Files modified" above) that goes slightly beyond the brief's literal text but stays within its own spirit ("write real error handling").
3. **Minimal `GET /api/projects/[projectId]/specs` list route** — added, per the brief's recommendation ("yes, add it"). Flagged explicitly here, in Dev Notes, per the brief's own instruction to surface this as a visible gap-filled-in rather than a silent addition: this route is not named in `context/feature-specs/28-spec-persistence-download.md`'s own three numbered implementation items or its "Check When Done" checklist.
4. **`Content-Disposition` filename convention** — `spec-{specId}.md`, per the brief's recommendation, adopted as-is.
5. **Multiple `ProjectSpec` rows per project** — allowed, per the brief's recommendation. No unique constraint on `projectId`, no overwrite-on-generate logic anywhere in `persistGeneratedSpec`. This is not versioned spec history (no version numbers, no diffing, no review/approval flow) — just independently generated, independently downloadable artifacts, matching `project-overview.md`'s own "Specs are persisted as files and linked to the project" text.

### Additional implementation notes (not brief Open Questions)

- **List route response shape**: `{ id, filename, createdAt }`, not `{ id, filePath, createdAt }`. The brief's own Open Questions #3 text offered "filePath or a derived filename" as two acceptable options; the derived filename was chosen deliberately so this route never puts a raw Blob URL in a response body anywhere in this diff — consistent with the download route's own "never the raw Blob URL" convention and with `lib/canvas-blob.ts`'s already-established precedent of never returning a raw blob URL to a client.
- **`GenerateSpecResult.specId`**: a minimal, non-brief-mandated addition to the trigger task's return shape (see "Files modified" above). Called out explicitly here so QA/PO can judge whether this crosses into invented scope — my own read is that it doesn't, since it adds no new deliverable, no new route, and no new persistence behavior; it only exposes an id the function already computes as part of doing the brief's own explicitly-required persistence step.
- **Orphaned-row cleanup on a Blob/Prisma-update failure**: also flagged explicitly for the same reason — a defensive addition inside `persistGeneratedSpec`'s own error path, not a new feature surface.

### Test coverage

- `lib/spec-blob.test.ts` — `specBlobPathname` determinism; missing-token errors for both `uploadSpecMarkdown`/`fetchSpecMarkdown` (put/get never called); `uploadSpecMarkdown` calls `put` with the exact pathname/content/`{ access: "private", contentType: "text/markdown", addRandomSuffix: false, allowOverwrite: true, token }` options; `fetchSpecMarkdown` returns `null` for a `null` `get` result, `null` for a non-200 status, the raw markdown string for a 200 response, and propagates (does not swallow) a genuine upstream `get` rejection.
- `app/api/projects/[projectId]/specs/route.test.ts` — 401/404/403 via `getProjectAccess`, each before any Prisma read; an empty list; a list of specs mapped to `{ id, filename, createdAt }` in the exact `findMany` order (newest first), with a dedicated assertion that the serialized response body never contains the substring `"blob"`.
- `app/api/projects/[projectId]/specs/[specId]/download/route.test.ts` — 401/404/403 via `getProjectAccess`, each before any Prisma read or Blob fetch; 404 for an unknown `specId`; 404 for a `specId` that exists but belongs to a different `projectId` (the brief's own explicit "verify the spec belongs to that project" requirement); 404 when the referenced Blob content is missing; 500 for a genuine upstream Blob failure; the success path asserting the exact `Content-Type`/`Content-Disposition` headers, the response body equal to the fetched markdown, and — directly exercising acceptance criterion 5/8 — that the response body never contains the substring `"blob.example"` (the mocked blob URL's own domain).
- `trigger/generate-spec.test.ts` (extended) — updated the existing happy-path/status-order/logging assertions for the new `specId` field and the extra `"processing"` status (`["start", "processing", "processing", "complete"]`); new "runGenerateSpec — persistence" block: the exact `create`/`uploadSpecMarkdown`/`update` call sequence and arguments; persistence is never attempted when Gemini itself fails; a Blob-upload failure deletes the placeholder row and rethrows (with an `error` status published); a Prisma `update` failure likewise deletes the placeholder row and rethrows; a secondary failure inside the cleanup `delete` call itself is logged (not swallowed, not rethrown in place of the original error).
- Full suite: 584/584 tests passing across 61 files (up from 558/58 at the end of spec 27), via `npx vitest run --no-file-parallelism`.

### Commands run

- `npx tsc --noEmit` — clean, no errors.
- `npx eslint .` — clean on every file this diff touches (spot-checked individually: `lib/spec-blob.ts`, `lib/spec-blob.test.ts`, `trigger/generate-spec.ts`, `trigger/generate-spec.test.ts`, both new route files and their test files — zero output). Repo-wide run surfaces 53 errors/1339 warnings, but every one of them is confined to generated `.trigger/tmp/*.mjs` build artifacts, the same pre-existing category already logged by specs 20/24/27 — none in this diff's own files.
- `npx vitest run --no-file-parallelism` — 584/584 passing across 61 files.
- `npx next build` — passing; `/api/projects/[projectId]/specs` and `/api/projects/[projectId]/specs/[specId]/download` both appear correctly in the route manifest as dynamic routes.
- `npx prisma migrate dev --name add_project_spec` — applied cleanly against the environment's real, provisioned Postgres database; `npx prisma generate` re-run afterward to refresh the generated client types `lib/prisma.ts` re-exports.

### Known limitations / deliberate deferrals

- **No live Blob smoke test of the new `specs/{projectId}/{specId}.md` pathname specifically.** `lib/canvas-blob.ts`'s `access: "private"` convention was already live-confirmed (the fix that landed ahead of this spec), and `lib/spec-blob.ts` reuses that exact configuration, but no one-off script has exercised a real `uploadSpecMarkdown`/`fetchSpecMarkdown` round trip against the live store in this pass. Recommended as a human smoke test (trigger a real `generate-spec` run end to end, confirm a `ProjectSpec` row and a fetchable blob both exist, then call the download route for real) before spec 29 builds a UI on top of these routes — not a blocker for this handoff, the same category of gap spec 22/23/27 shipped with before their own post-PASS live-verification rounds.
- **No live Trigger.dev run of the now-persistence-aware `generate-spec` task triggered in this pass** — same recommended-not-blocking human smoke test as above covers this too; the task's Prisma/Blob calls are fully unit-tested with mocked boundaries, matching this repo's established testing convention, but not yet exercised against the live Trigger.dev project.
- Everything else named in the brief's own Out-of-scope callouts (spec list/preview UI, wiring the "Generate Spec" button, storing spec content in Prisma, modifying existing canvas persistence, versioned spec history, rate limiting, billing/enterprise/mobile) is genuinely untouched — confirmed via `git status`/`git diff` that no `components/*`, `lib/canvas-blob.ts`, or `app/api/projects/[projectId]/canvas/route.ts` file appears anywhere in this diff.
