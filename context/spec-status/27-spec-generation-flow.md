Source spec: `context/feature-specs/27-spec-generation-flow.md`

## Analyst Brief

### Scope statement

Build the backend-only plumbing for AI-powered spec generation: a `POST /api/ai/spec` route that authenticates the caller, resolves project access from a client-supplied `roomId` (never a client-supplied `projectId`), triggers a new `generate-spec` Trigger.dev task, and records a `TaskRun`; a `POST /api/ai/spec/token` route that issues a run-scoped Trigger.dev public token to the run's owner; and a `trigger/generate-spec.ts` task that validates its input with Zod, calls Gemini via `@ai-sdk/google` to turn the current chat history + canvas graph into a Markdown technical spec, and returns that Markdown as its task output. No frontend wiring, no spec editor UI, and no persistence of the generated spec happen in this unit — this is the design-agent-API/-logic pattern (specs 22/23) applied to spec generation, structurally parallel but not bundled with it.

### Concrete deliverables

- `package.json`/`package-lock.json` — **no new dependency expected.** `@trigger.dev/sdk` (spec 22), `@ai-sdk/google`/`ai` (spec 23), and `zod` (spec 25) are all already real, installed production dependencies. Confirmed by reading `package.json` directly. This is the first spec in the design/spec-generation chain that shouldn't need to touch this file at all — see Dependencies below.
- `lib/generate-spec-ai.ts` (new) — Gemini interpretation module, mirroring `lib/design-agent-ai.ts`'s lazy-provider-instantiation pattern (`GEMINI_MODEL_ID`, a `globalThis`-cached `createGoogleGenerativeAI` provider, `requireGeminiApiKey`). Unlike `design-agent-ai.ts` (which uses `generateObject` + `jsonSchema()` to produce a bounded action list), this module should use the `ai` package's `generateText` (or `streamText`) — the raw spec text's own "keep the task output as plain Markdown" instruction names free-form text output, not a structured schema. See Open Questions #4.
- `trigger/generate-spec.ts` (new) — mirrors `trigger/design-agent.ts`'s shape: exports `GENERATE_SPEC_TASK_ID`, a `GenerateSpecPayload` interface (`projectId`, `roomId`, `chatHistory`, `nodes`, `edges`), a `runGenerateSpec(payload)` function exported separately from the `task(...)` registration (same "unit-testable without Trigger.dev's runtime" reasoning spec 22/23 already established), and `generateSpecTask = task({ id: GENERATE_SPEC_TASK_ID, run: runGenerateSpec })`. Validates its payload with a real Zod schema (`z.object(...)`) before calling Gemini — the raw spec text's own explicit "validates input with Zod" instruction, unlike spec 22/23's manual-type-guard precedent for the design-agent task. Follows `trigger/design-agent.ts`'s existing try/catch/`logger.error`/rethrow convention for error handling.
- `lib/trigger.ts` (modified) — add `triggerGenerateSpec(payload)`, mirroring `triggerDesignAgent` exactly. Also update the existing token-issuing function so it can be reused for both design and spec runs rather than duplicated — see Open Questions #5 for the exact shape.
- `app/api/ai/spec/route.ts` (new) — `POST`: auth (`getCallerIdentity`) → parse/validate body with Zod (`{ roomId, chatHistory, nodes, edges }` — **no `projectId` field accepted from the client**, per the raw spec text's explicit "Do not trust a client-supplied `projectId`" instruction) → `getProjectAccess(roomId)` (owner-or-collaborator, same gate as `/api/ai/design`) → `triggerGenerateSpec({ projectId: roomId, roomId, chatHistory, nodes, edges })` (the task's own `projectId` field is populated from the already-access-checked `roomId`, never from anything the client separately claims) → `prisma.taskRun.create` (reusing the existing `TaskRun` model — see Open Questions #6) → returns `{ runId }`.
- `app/api/ai/spec/token/route.ts` (new) — `POST`: auth → parse/validate `{ runId }` with Zod → `TaskRun` lookup → `TaskRun.userId`-scoped ownership check (identical precedence to `/api/ai/design/token`: 401 → 400 → 404 → 403 → 502) → issues a Trigger.dev public token with a 1-hour expiration (already an established Architecture Decision — `progress-tracker.md` records spec 22's design-token route was updated to match spec 27's 1-hour requirement, not the other way around) → returns `{ token }`.
- Tests: `lib/generate-spec-ai.test.ts`, `trigger/generate-spec.test.ts`, `app/api/ai/spec/route.test.ts`, `app/api/ai/spec/token/route.test.ts` (all new), `lib/trigger.test.ts` (extended for the new/generalized functions) — following this repo's established `vi.mock`/`vi.hoisted` conventions for Clerk `auth()`, the Prisma singleton, and the Trigger.dev/Gemini SDK boundaries.
- No `components/*` file changes anywhere in this diff — this spec's own Scope Limit ("Do not add frontend logic"). `components/editor/specs-tab.tsx`'s "Generate Spec" button stays exactly as inert as spec 20 left it; that file's own docblock already names specs 27/29 as the ones that eventually wire it up, and spec 29 (not this spec) is what "reuses this existing sidebar layout."
- No `prisma/schema.prisma` change expected — see Open Questions #6.

### Acceptance criteria

1. `POST /api/ai/spec` accepts `{ roomId, chatHistory, nodes, edges }` in the request body. No `projectId` field is read from or trusted in the request body anywhere in this diff — project access and the task's own `projectId` value are both derived from the already-authenticated, already-access-checked `roomId` only.
2. `POST /api/ai/spec` returns 401 for an unauthenticated caller, and 404/403 (via `getProjectAccess(roomId)`) for a missing or non-member project, before any Trigger.dev call or Prisma write happens.
3. `POST /api/ai/spec`, given a valid authenticated owner-or-collaborator request, triggers the `generate-spec` task through the Trigger.dev SDK, creates one `TaskRun` row (`runId`, `projectId` = the validated `roomId`, `userId`, `createdAt`), and returns `{ runId }`.
4. `POST /api/ai/spec/token` returns 401 for an unauthenticated caller, and does not return a usable token for a `runId` that does not exist or does not belong to the caller (`TaskRun.userId` check, matching `/api/ai/design/token`'s precedent).
5. `POST /api/ai/spec/token`, given a `runId` owned by the caller, issues a Trigger.dev public token scoped to that run only, with a 1-hour expiration, and returns it to the client.
6. `trigger/generate-spec.ts` exports a `generate-spec` task accepting `{ projectId, roomId, chatHistory, nodes, edges }`, validates that payload with a real Zod schema before calling Gemini (rejecting malformed input, e.g. non-array `nodes`/`edges` or a wrong-shaped `chatHistory` entry, without ever reaching the AI call), calls Gemini via `@ai-sdk/google` to generate Markdown content from the chat/canvas context, and returns the generated Markdown as its task output.
7. No frontend/`components/*` file is changed anywhere in this diff.
8. No spec content is persisted anywhere (no Prisma write beyond the `TaskRun` row, no Vercel Blob upload, no filesystem write) — the task's Markdown output is returned only, never stored.
9. No new AI provider abstraction is introduced — `lib/generate-spec-ai.ts` reuses the exact same `@ai-sdk/google`/Gemini lazy-instantiation pattern `lib/design-agent-ai.ts` already established (same `GEMINI_MODEL_ID`/`globalThis`-cached-provider convention).
10. `types/canvas.ts` (`CanvasNode`/`CanvasEdge`/`CanvasNodeData`/`CanvasEdgeData`) and `types/tasks.ts` (`AiChatMessage`/`AiChatMessageSchema`) are unmodified anywhere in this diff — this spec only imports and validates against them.
11. Request handlers stay thin — the Gemini call and Markdown-generation logic live only in `trigger/generate-spec.ts`/`lib/generate-spec-ai.ts`, never inline in either route handler (`architecture-context.md` Invariant 1: "Request handlers do not run long-lived AI work").
12. `npm run build` passes, along with this repo's standard gates (`npx tsc --noEmit`, `npx eslint .`, `npx vitest run`).

### Dependencies

- **Spec 22 (Design Agent API)** — the `TaskRun` Prisma model, `lib/trigger.ts`'s lazy-instantiation pattern, and the `POST /api/ai/design`/`POST /api/ai/design/token` route shape this spec mirrors directly. **Complete.**
- **Spec 23 (Design Agent Logic)** — `lib/design-agent-ai.ts`'s Gemini/`@ai-sdk/google` lazy-provider pattern this spec's own `lib/generate-spec-ai.ts` reuses. **Complete, live-verified against a real Gemini account** (see `context/spec-status/23-design-agent-logic.md`'s Post-PASS section) — the model ID/`maxOutputTokens` lessons learned there apply directly to this spec's own Gemini call.
- **Spec 25 (Sidebar Chat Feed)** — `types/tasks.ts`'s `AiChatMessage`/`AiChatMessageSchema`, the shape this spec's `chatHistory` input should reuse unmodified, and the `zod` dependency this spec's own Zod-validation requirement relies on already being installed. **Complete.**
- **Spec 11 (Base Canvas)** — `types/canvas.ts`'s `CanvasNode`/`CanvasEdge` shapes this spec's `nodes`/`edges` input should reuse. **Complete.**
- **Spec 20 (AI Sidebar Shell)** — `components/editor/specs-tab.tsx`'s inert "Generate Spec" button, which this spec deliberately does not wire up (that file's own docblock already anticipates this spec and spec 29 splitting generation from persistence/UI). **Complete, untouched by this spec.**
- `progress-tracker.md`'s Architecture Decisions — the 1-hour token expiration is already a resolved, shared decision across specs 22 and 27 (not something this spec needs to newly decide). Its "Deferred — Production Hardening" section also already names `/api/ai/spec` alongside `/api/ai/design` for a future rate-limiting pass, confirming this spec is not expected to add rate limiting itself.

### Open questions

1. **What "updates run metadata/status for realtime tracking" concretely requires.** The raw spec text names this as a task responsibility but doesn't specify a mechanism. Two real candidates exist in this codebase: (a) `trigger/design-agent.ts`'s existing pattern of broadcasting to the Liveblocks `ai-status-feed` event (`lib/design-agent-room.ts#broadcastDesignAgentStatus`), which `types/tasks.ts`'s `AiStatusMessage` docblock already notes was kept "generic enough for spec generation's own status broadcasts later"; or (b) Trigger.dev's own native run-metadata mechanism (`metadata.set(...)` from `@trigger.dev/sdk`, consumable client-side through the already-installed `@trigger.dev/react-hooks#useRealtimeRun`, spec 26, with no Liveblocks involvement at all). **Recommendation: (b), Trigger.dev's own metadata mechanism.** Reasoning: the raw text's own wording — "run metadata" — is Trigger.dev's own vocabulary, not how spec 23's text described the Liveblocks mechanism ("broadcast AI status feed events"); this spec's Scope Limits explicitly forbid "frontend logic," and a Liveblocks broadcast is only observable through frontend wiring that doesn't exist for this flow yet; and reusing the single, latest-message-only `ai-status-feed` for a second, structurally different producer (keyed by `roomId` access but not tied to any canvas mutation) risks a viewer being unable to tell which flow's status they're currently seeing. Flagging as a recommendation for the Senior Developer to confirm, not a settled decision — this is the single largest interpretive gap in the spec's text.
2. **Exact Zod shape for `nodes`/`edges`.** `CanvasNode`/`CanvasEdge` are `@xyflow/react`'s `Node<T>`/`Edge<T>` generics with several fields (`id`, `position`, `data`, `width`, `height`, `selected`, etc.) that aren't all relevant to summarizing a graph for Markdown generation. Recommendation: validate a narrower structural subset sufficient for the prompt (id/label/shape/position for nodes; id/source/target/label for edges) — mirroring `lib/design-agent-ai.ts`'s own `DesignAgentGraphNode`/`DesignAgentGraphEdge` summary types, which already take exactly this approach against the same underlying `CanvasNode`/`CanvasEdge` shapes — rather than a brittle, full 1:1 Zod replica of the React Flow type.
3. **`generateText` vs. `generateObject`.** `lib/design-agent-ai.ts` uses `generateObject` + `jsonSchema()` because its output is a bounded, structured action list. This spec's output is free-form Markdown — the raw spec text's own "keep the task output as plain Markdown" instruction. Recommendation: use the `ai` package's `generateText` (not `generateObject`), returning `{ text }` as the task's Markdown output directly — no schema to enforce on the model's response shape.
4. **Whether `lib/trigger.ts`'s token-issuing function should be generalized or duplicated.** `createDesignRunToken(runId)` (spec 22) has no design-specific logic in its own body today — it only scopes a public token to a given `runId` with a fixed expiration. Recommendation: generalize it into a task-agnostic `createRunToken(runId)` and reuse it for both `/api/ai/design/token` and `/api/ai/spec/token`, rather than adding a byte-for-byte duplicate function — `code-standards.md`'s "fix root causes — do not layer workarounds" argues against the duplicate. Flagged as a recommendation touching a spec-22-owned file, not a unilateral decision; renaming is a minor, mechanical, non-behavioral change (the token route callers only care about the return value).
5. **Whether `TaskRun` needs a schema change** (e.g. a `type`/`taskId` discriminator distinguishing a design-agent run from a spec-generation run). Recommendation: no schema change. `TaskRun`'s own docblock already anticipates "future task types," ownership is enforced purely via `userId` regardless of which task produced a given `runId`, and each `runId` is already globally unique per Trigger.dev task invocation — nothing in either spec's acceptance criteria depends on distinguishing run *kind* after the fact.
6. **Whether an empty `nodes`/`edges` graph should be rejected or passed through as-is.** Nothing in the raw spec text or `project-overview.md` describes a minimum-content gate before spec generation. Recommendation: allow it through — Gemini can be prompted to produce a minimal/placeholder spec noting the empty canvas, rather than the route/task inventing a new validation rule not asked for anywhere.
7. **`chatHistory`'s exact type.** Recommendation: `AiChatMessage[]` (`types/tasks.ts`, spec 25), reused unmodified, giving the model the room's full sender/role/content/timestamp chat context — consistent with the raw spec text's own "generates a Markdown technical spec from the canvas and chat context" phrasing and this spec's own Scope Limit against changing existing data models.

### Out-of-scope callouts

- **Wiring the "Generate Spec" button** (`components/editor/specs-tab.tsx`) — this spec's own explicit Scope Limit ("Do not add frontend logic"). The button stays exactly as inert as spec 20 left it.
- **Spec editor/viewer UI** — this spec's own explicit Scope Limit ("Do not create spec editor UI"). The one static demo card in `specs-tab.tsx` is untouched.
- **Persisting the generated spec** (Vercel Blob upload, a `Project.filePath`-style Prisma record/link) — this spec's own explicit Scope Limit ("Do not store the final spec in this unit"). The task only returns Markdown as task output; nothing in this diff writes it to Blob, links it to the project, or touches the database beyond the `TaskRun` row. `specs-tab.tsx`'s own docblock names spec 29 as the one that wires "real generation and persistence" together — this spec delivers the generation half only.
- **Deriving access from a client-supplied `projectId`** — this spec's own explicit Scope Limit and acceptance criterion 1; access must come from the authenticated user + `roomId` only.
- **A new AI provider abstraction** — this spec's own explicit Scope Limit; reuses `@ai-sdk/google` + Gemini exactly as `lib/design-agent-ai.ts` already established.
- **Any change to existing canvas (`types/canvas.ts`) or chat (`types/tasks.ts`) data models** — this spec's own explicit Scope Limit.
- **Canvas/Liveblocks room mutation of any kind** — the task only reads the `nodes`/`edges`/`chatHistory` already passed in the request body; unlike `trigger/design-agent.ts` (which mutates Liveblocks Storage), this task produces a read-only Markdown artifact and touches no Liveblocks room state (beyond the still-open metadata/status question, Open Questions #1, which itself was recommended toward a non-Liveblocks mechanism).
- **Rate limiting on `/api/ai/spec`** — already logged in `progress-tracker.md`'s "Deferred — Production Hardening" section, which explicitly names both `/api/ai/design` and `/api/ai/spec`; not this spec's job.
- **Billing/subscriptions, enterprise permission tiers, versioned spec history, production object storage migration, mobile apps** — `project-overview.md`'s Out of Scope wall; none of this spec's work touches any of them.

Brief ready for Senior Developer at `context/spec-status/27-spec-generation-flow.md`.

## Dev Notes

### Files added

- `lib/generate-spec-ai.ts` (new) — Gemini interpretation module mirroring `lib/design-agent-ai.ts`'s lazy-provider-instantiation pattern (`GEMINI_MODEL_ID = "gemini-3.6-flash"`, a `globalThis`-cached `createGoogleGenerativeAI` provider — cached under a distinct key, `specGenGoogleProvider`, so the two modules' lazy caches stay independently testable/resettable — and `requireGeminiApiKey`). Uses `ai`'s `generateText` (not `generateObject`/`jsonSchema()`), per Open Questions #3 — no schema enforced on the response, the model's raw text is returned as-is. Exports `GenerateSpecGraphNode`/`GenerateSpecGraphEdge` (the narrow structural graph-summary types from Open Questions #2 — `{ id, label, shape, x, y }`/`{ id, sourceNodeId, targetNodeId, label? }`, not a 1:1 `CanvasNode`/`CanvasEdge` replica) and `GenerateSpecInput`/`generateSpecMarkdown`. `maxOutputTokens: 8192` set from the start, applying spec 23's own live-verified lesson (`gemini-3.6-flash` reasons before responding and a low/zero token budget truncates output) proactively rather than waiting to rediscover it.
- `trigger/generate-spec.ts` (new) — `GENERATE_SPEC_TASK_ID = "generate-spec"`, `GenerateSpecPayload`/`GenerateSpecResult` interfaces, `runGenerateSpec(payload)` (exported separately from the `task(...)` registration, same unit-testability reasoning as `runDesignAgent`), and `generateSpecTask = task({ id: GENERATE_SPEC_TASK_ID, run: runGenerateSpec })`. Validates the payload with a real Zod schema (`GenerateSpecPayloadSchema`, built from exported `GenerateSpecGraphNodeSchema`/`GenerateSpecGraphEdgeSchema` plus `types/tasks.ts`'s existing `AiChatMessageSchema`) via `.parse(...)` before Gemini is ever called — a malformed payload throws synchronously, before any status is published or `generateSpecMarkdown` runs. Publishes `start`/`processing`/`complete`/`error` status through Trigger.dev's own native `metadata.set` run-metadata mechanism (Open Questions #1's recommended option (b)), reusing `types/tasks.ts`'s `AiStatusStage`/`AiStatusMessage` shape for the payload only (a different transport, the same vocabulary spec 23/24 established) — no Liveblocks broadcast, no Liveblocks import anywhere in this file. Follows `runDesignAgent`'s try/catch/`logger.error`/rethrow convention, including the "a secondary failure while reporting the error must not mask the original" pattern (the error-status `metadata.set` call is itself wrapped in its own try/catch).
- `app/api/ai/spec/route.ts` (new) — `POST`: `getCallerIdentity` → parse JSON → `SpecRequestBodySchema.safeParse` (Zod; `{ roomId, chatHistory, nodes, edges }` only — reuses `trigger/generate-spec.ts`'s own exported `GenerateSpecGraphNodeSchema`/`GenerateSpecGraphEdgeSchema` rather than a second, potentially-drifting copy) → `getProjectAccess(body.roomId)` → `triggerGenerateSpec({ projectId: body.roomId, roomId: body.roomId, ... })` → `prisma.taskRun.create` → `{ runId }`. No `projectId` field exists anywhere in the Zod schema, so even a client that sends one has it silently dropped by `.safeParse` before anything downstream ever sees it (acceptance criterion 1; covered directly by a dedicated test).
- `app/api/ai/spec/token/route.ts` (new) — `POST`: `getCallerIdentity` → parse JSON → `TokenRequestBodySchema.safeParse` (Zod; `{ runId }`) → `TaskRun` lookup → `TaskRun.userId`-scoped ownership check → `createRunToken` (1-hour expiration) → `{ token }`. Byte-for-byte the same failure precedence as `/api/ai/design/token`.
- Tests: `lib/generate-spec-ai.test.ts`, `trigger/generate-spec.test.ts`, `app/api/ai/spec/route.test.ts`, `app/api/ai/spec/token/route.test.ts` (all new).

### Files modified

- `lib/trigger.ts` — added `triggerGenerateSpec(payload)` (mirrors `triggerDesignAgent` exactly) and a new `TriggeredGenerateSpecRun` interface. Generalized spec 22's `createDesignRunToken(runId)` into a task-agnostic `createRunToken(runId)` per Open Questions #4's recommendation — same body (`auth.createPublicToken({ scopes: { read: { runs: [runId] } }, expirationTime: "1h" })`), no behavior change, callers only ever cared about the return value. `triggerDesignAgent`/`TriggeredDesignRun` untouched.
- `app/api/ai/design/token/route.ts` — updated its import/call site from `createDesignRunToken` to the renamed `createRunToken`; nothing else in this file changed (still its own manual `isValidTokenRequestBody` type guard, not Zod — that migration wasn't asked for by this spec and wasn't made).
- `lib/trigger.test.ts` (extended) — added `triggerGenerateSpec` coverage (missing-key error, exact `tasks.trigger("generate-spec", payload)` call, upstream-failure propagation) and renamed the token-function tests/mocks from `createDesignRunToken` to `createRunToken`, plus a new "task-agnostic" case exercising the same `createRunToken` call with a `generate-spec`-shaped `runId`.
- `app/api/ai/design/token/route.test.ts` (extended, rename only) — mock/variable names updated from `createDesignRunTokenMock` to `createRunTokenMock`; no behavioral or assertion changes.
- `context/architecture-context.md` — new "Realtime Conventions" bullet recording the run-metadata-not-Liveblocks-broadcast decision for spec-generation status, so spec 29 (or any future frontend-wiring spec) doesn't have to re-derive it.
- `context/progress-tracker.md` — "Current Phase"/"Current Goal"/"In Progress"/"Next Up" updated for this spec; new "Architecture Decisions" bullet cross-referencing the same run-metadata decision and the `createRunToken` generalization. (Also cleaned up a stale "In Progress" entry that still described spec 26 as "QA pass not yet started" after spec 26 had already been fully completed and moved to the "Completed" section in an earlier commit — a pre-existing tracker inconsistency unrelated to this spec's own diff, fixed in passing since I was already editing this section.)

### No `prisma/schema.prisma` change

Confirmed directly: `TaskRun` (spec 22) already has exactly the fields this spec needs (`runId`, `projectId`, `userId`, `createdAt`) with no `type`/`taskId` discriminator, and nothing in this spec's acceptance criteria depends on distinguishing a design-agent run from a spec-generation run after the fact (Open Questions #5). No migration was created or run.

### Skills used

- No skill under `.claude/skills/` matched this spec's actual work. Checked `prisma-client-api`/`prisma-cli` (no schema or query change — `prisma.taskRun.create`/`findUnique` are byte-for-byte the same calls spec 22 already used) and `liveblocks-best-practices` (explicitly not used — Open Questions #1 was resolved toward Trigger.dev's own metadata mechanism specifically to avoid a second, structurally different Liveblocks producer). `clerk-nextjs-patterns` wasn't consulted fresh either — `getCallerIdentity()` (`lib/project-access.ts`) is pre-existing, unmodified infrastructure this spec only calls, not new Clerk integration work.

### Key decisions (brief's Open Questions)

1. **Run metadata/status mechanism** — Trigger.dev's own native `metadata.set` (option (b)), per the brief's recommendation. No Liveblocks broadcast, no `lib/design-agent-room.ts` import, no room mutation of any kind anywhere in `trigger/generate-spec.ts`.
2. **`nodes`/`edges` Zod shape** — the narrow structural subset the brief recommended: `{ id, label, shape, x, y }` for nodes, `{ id, sourceNodeId, targetNodeId, label? }` for edges (mirroring `DesignAgentGraphNode`/`DesignAgentGraphEdge`'s own field names, not the shorthand "source"/"target" wording used loosely in the brief's own prose, for internal consistency with the one existing precedent in this codebase). Defined in `lib/generate-spec-ai.ts` as plain TypeScript interfaces (the AI-facing summary types, no Zod), with the actual Zod schemas living in `trigger/generate-spec.ts` and re-exported for the route to share — the same lib/AI-module-vs-trigger/validation-module split `design-agent-ai.ts`/`design-agent.ts` already established, just with real Zod schemas this time instead of JSON Schema.
3. **`generateText` vs `generateObject`** — `generateText`, per the brief's recommendation. `{ text }` is returned directly as the task's Markdown output; no schema enforced on the model's response shape.
4. **Generalizing vs. duplicating the token function** — generalized `createDesignRunToken` into `createRunToken`, per the brief's recommendation, and updated both callers (`/api/ai/design/token`, `/api/ai/spec/token`) to use it. Verified via `git diff`/direct read that this is a pure rename with an unchanged body — no behavior change to spec 22's already-shipped, already-tested token-issuance logic.
5. **`TaskRun` schema change** — none, per the brief's recommendation. Confirmed the existing model already has every field this spec needs.
6. **Empty `nodes`/`edges`/`chatHistory`** — allowed through as-is, per the brief's recommendation. `generateSpecMarkdown`'s prompt explicitly instructs the model to note an empty canvas/no chat history and produce a short placeholder document rather than erroring, and a dedicated route test (`"accepts an empty nodes/edges/chatHistory graph"`) and task test (`"accepts an empty nodes/edges graph and empty chat history, passing them through as-is"`) both confirm no artificial minimum-content gate was added anywhere in the diff.
7. **`chatHistory` type** — `AiChatMessage[]` (`types/tasks.ts`, spec 25), reused completely unmodified — confirmed via `git diff` that `types/tasks.ts` does not appear anywhere in this spec's diff.

### Additional implementation notes (not brief Open Questions)

- **Zod schema sharing between the route and the task**: rather than the route (`app/api/ai/spec/route.ts`) defining its own near-duplicate `nodes`/`edges` Zod schemas, it imports and reuses `trigger/generate-spec.ts`'s own exported `GenerateSpecGraphNodeSchema`/`GenerateSpecGraphEdgeSchema` directly. This means the route's request-body contract and the task's own payload contract can never silently drift apart — a stricter reading of `code-standards.md`'s "fix root causes — do not layer workarounds" than the brief's text explicitly asked for, but a small, mechanical, low-risk choice (both are pure, side-effect-free Zod value exports; no Liveblocks/AI-provider code executes at import time either way, matching the already-proven-safe lazy-instantiation posture `next build`'s page-data collection already relies on for `lib/trigger.ts`/`trigger/design-agent.ts`).
- **`GenerateSpecPayload` as a literal `interface`, not a `z.infer` type alias**: the brief's own concrete-deliverables text names "a `GenerateSpecPayload` interface" specifically (mirroring `DesignAgentPayload`'s own plain-interface shape, no Zod involvement), and `code-standards.md` says "use `interface` for object contracts" — so unlike `types/tasks.ts`'s own `AiChatMessage` (derived via `z.infer<typeof AiChatMessageSchema>`, spec 25's own established pattern), this spec keeps the interface and the Zod schema as two independently-declared, structurally-matching definitions rather than deriving one from the other. `runGenerateSpec`'s own `GenerateSpecPayloadSchema.parse(payload)` call is what keeps them from silently drifting apart in practice — a real runtime check, not just a comment-level assertion.
- **Validation happens outside the task's `try`/`catch` block**: `GenerateSpecPayloadSchema.parse(payload)` runs before the `try` block starts, so a validation failure throws straight up to Trigger.dev's own runtime without ever publishing a `start` status via `metadata.set` — a malformed payload is a caller/integration bug, not a "the run genuinely started and then failed" case, so no misleading in-progress status is ever published for it.

### Test coverage

- `lib/generate-spec-ai.test.ts` — missing-`GEMINI_API_KEY` handled error (provider/`generateText` never called); lazy/cached provider instantiation across two calls; raw Gemini text returned unmodified; node/edge graph summary and chat history both present in the prompt text; an empty chat history produces the "(no chat history yet)" placeholder text rather than an empty section; a genuine upstream Gemini failure propagates rather than being swallowed.
- `trigger/generate-spec.test.ts` — happy path (validates, calls `generateSpecMarkdown` with exactly `{ chatHistory, nodes, edges }`, returns `{ roomId, projectId, markdown }`); `start`/`processing`/`complete` status published in order via the mocked `metadata.set`; an empty nodes/edges/chatHistory payload passes through as-is; completion is logged with room/project ids and markdown length; five distinct payload-validation-rejection cases (non-array `nodes`, non-array `edges`, malformed `chatHistory` entry, malformed node missing `x`/`y`, empty `roomId`) each confirming `generateSpecMarkdown` (and, for the first, `metadata.set`) is never called; Gemini failure publishes an `error` status and rethrows with the right logged fields; a secondary failure inside the error-status `metadata.set` call itself doesn't mask the original error (and is separately logged); task registration (`id`/`run` captured at the mock's one import-time call, matching `design-agent.test.ts`'s own precedent for this exact gotcha).
- `app/api/ai/spec/route.test.ts` — 401 before body/Trigger.dev/Prisma; 400 for invalid JSON, a missing `roomId` (with and without an attacker-supplied `projectId` alongside it), non-array `nodes`, non-array `edges`, a wrong-shaped `chatHistory` entry, and a malformed node; an empty nodes/edges/chatHistory body is accepted; 404/403 via `getProjectAccess`; a dedicated test asserting a client-supplied `projectId` is silently dropped and never reaches `getProjectAccess`/`triggerGenerateSpec`/the `TaskRun` write (all keyed on `roomId` only) — the most direct exercise of acceptance criterion 1; the standard success path (exact `triggerGenerateSpec` payload shape, exactly one `TaskRun.create` call, `{ runId }` response); 502 when triggering fails (Prisma write skipped); 500 when the Prisma write fails after a successful trigger. Deliberately left `trigger/generate-spec.ts` unmocked in this file (see "Additional implementation notes" above) so the route's Zod validation is exercised against the real, shared schema objects, not a re-implemented stub.
- `app/api/ai/spec/token/route.test.ts` — 401 before body/Prisma/Trigger.dev; 400 for invalid JSON and a non-string `runId`; 404 for an unknown `runId`; 403 when the run belongs to a different caller; success path (`{ token }`, `createRunToken` called with the exact `runId`); 502 when token issuance fails upstream.
- `lib/trigger.test.ts` (extended) — added `triggerGenerateSpec` (missing-key error, exact SDK call shape, upstream-failure propagation) and renamed `createDesignRunToken` coverage to `createRunToken`, plus a new case confirming the same function issues an equally-scoped token for a `generate-spec`-run `runId` (task-agnostic, acceptance criterion 5/Open Questions #5).
- `app/api/ai/design/token/route.test.ts` (extended, rename only) — same test bodies, `createDesignRunTokenMock` renamed to `createRunTokenMock` throughout.
- Full suite: 558/558 tests passing across 58 files (up from 512/54 at the end of spec 26), via `npx vitest run --no-file-parallelism`.

### Commands run

- `npx tsc --noEmit` — clean, no errors.
- `npx eslint .` — clean on every file this diff touches (two `no-unused-vars` warnings caught and fixed in `app/api/ai/spec/route.test.ts` during this pass — see below). Remaining errors/warnings repo-wide are entirely pre-existing, confined to generated `.trigger/tmp/` build artifacts and third-party skill templates, untouched by this diff (same category already logged by specs 20/24).
- `npx vitest run --no-file-parallelism` — 558/558 passing across 58 files.
- `npx next build` — passing (see below for confirmation this ran with the real `GEMINI_API_KEY`/`TRIGGER_SECRET_KEY` already present in this environment's `.env.local` from spec 23's/the Trigger.dev setup's post-PASS provisioning — not exercised with them absent this time, since that lazy-instantiation posture was already proven safe for this exact module shape by specs 22/23).

### Known limitations / deliberate deferrals

- **No live Gemini smoke test for `generateSpecMarkdown` specifically.** Spec 23's own Post-PASS section live-verified `gemini-3.6-flash`/`maxOutputTokens: 8192` against a real Gemini account for the design-agent's `generateObject` call; this spec applies the same model ID/token-budget lesson proactively to a `generateText` call, but that specific call path (free-form Markdown output, not structured JSON) has not itself been exercised against a live Gemini account in this pass. Recommended as a human smoke test (trigger a real `generate-spec` run end to end and read back the run's Markdown output from the Trigger.dev dashboard) before spec 29 builds the persistence/UI layer on top of it — not a blocker for this handoff, consistent with how spec 22 itself shipped without live verification before spec 23's follow-up round.
- **No live Trigger.dev run of `generate-spec` triggered in this pass** — the task is registered and unit-tested, but (unlike spec 23's Post-PASS section) no one-off script triggered a real run against the already-provisioned Trigger.dev project in this environment. Same recommended-not-blocking human smoke test as above covers this too.
- Everything else named in the brief's own Out-of-scope callouts (frontend wiring, spec editor/viewer UI, persistence, rate limiting, billing/enterprise/versioning/mobile) is genuinely untouched — confirmed via `git status`/`git diff` that no `components/*` file appears anywhere in this diff.


## QA Report

**Verdict: FAIL** (one non-blocking, docs-only issue -- see Issues found below)

### Mechanical gate (independently reproduced, not trusted from the Dev report)

| Check | Result |
|---|---|
| `npx tsc --noEmit` | Clean, no errors. |
| `npx eslint .` | Clean on every file this diff touches (spot-checked individually: `lib/generate-spec-ai.ts`, `trigger/generate-spec.ts`, `app/api/ai/spec/route.ts`, `app/api/ai/spec/token/route.ts`, `lib/trigger.ts`, `app/api/ai/design/token/route.ts`, and all six new/extended test files -- zero output). Repo-wide run does surface 53 errors/1339 warnings, but 100% of them are confined to generated `.trigger/tmp/*.mjs` build artifacts, matching the pre-existing, already-logged category from specs 20/24 -- none in this diff's own files. |
| `npx vitest run --no-file-parallelism` | 558/558 passing across 58 files (matches the Dev's own "Commands run"/"Test coverage" sections; see Housekeeping note below re: a stale "60 files" figure elsewhere in progress-tracker.md). |
| `npx next build` | Succeeds. `/api/ai/spec` and `/api/ai/spec/token` both appear correctly in the route manifest as dynamic routes. |

### Acceptance criteria checklist (against `context/feature-specs/27-spec-generation-flow.md` + the brief's own numbered list)

1. **PASS** -- `SpecRequestBodySchema` (`app/api/ai/spec/route.ts`) has no `projectId` field; a client-supplied `projectId` is silently stripped by Zod's default "strip unknown keys" behavior on `z.object(...)`. `getProjectAccess(body.roomId)` and `triggerGenerateSpec({ projectId: body.roomId, roomId: body.roomId, ... })` both derive exclusively from the validated `roomId`. Directly exercised by a dedicated test ("derives both project access and the triggered task's projectId from roomId only, never a client-supplied projectId") that passes an attacker-controlled `projectId` in the body and asserts it never reaches `getProjectAccess`/`triggerGenerateSpec`/`prisma.taskRun.create`.
2. **PASS** -- 401 happens before body parsing (`getCallerIdentity()` checked first in `POST`); 404/403 come from `getProjectAccess(roomId)`, both before `triggerGenerateSpec`/`prisma.taskRun.create` are ever called (confirmed by the route code and by tests asserting `triggerGenerateSpecMock`/`prismaMock.taskRun.create` were not called in each of these paths).
3. **PASS** -- happy path triggers `generate-spec` via `triggerGenerateSpec`, creates exactly one `prisma.taskRun.create` call with `{ runId, projectId: roomId, userId }`, and returns `{ runId }`. Verified directly in code and by the route's own success-path test.
4. **PASS** -- 401 before touching Prisma/Trigger.dev; unknown `runId` -> 404; `runId` owned by a different caller -> 403 (`TaskRun.userId !== identity.userId`). All three paths return before `createRunToken` is called.
5. **PASS** -- `createRunToken(runId)` in `lib/trigger.ts` calls `auth.createPublicToken({ scopes: { read: { runs: [runId] } }, expirationTime: "1h" })` -- confirmed the literal `"1h"` constant and the single-run scope; returned directly as `{ token }`.
6. **PASS** -- `GenerateSpecPayloadSchema.parse(payload)` runs before the task's `try` block, i.e. before Gemini or any `metadata.set` call. Confirmed the schema rejects non-array `nodes`/`edges`, a malformed `chatHistory` entry, and a node missing `x`/`y` -- each covered by a dedicated test asserting `generateSpecMarkdownMock` was never called. `generateSpecMarkdown` genuinely calls Gemini via `@ai-sdk/google`'s `generateText` and returns raw Markdown text as task output (`{ roomId, projectId, markdown }`).
7. **PASS** -- confirmed via `git diff main...HEAD --name-only` that no path under `components/` appears anywhere in the 15-file diff.
8. **PASS** -- no Prisma write beyond the one `taskRun.create` call in the route; `runGenerateSpec` only returns `{ roomId, projectId, markdown }` as task output, no Blob/filesystem write anywhere in `trigger/generate-spec.ts` or `lib/generate-spec-ai.ts`.
9. **PASS** -- `lib/generate-spec-ai.ts` reuses the same `GEMINI_MODEL_ID`/lazy-`globalThis`-cached-`createGoogleGenerativeAI`/`requireGeminiApiKey` shape `lib/design-agent-ai.ts` already established, with only a distinct cache key (`specGenGoogleProvider`) to keep the two lazy caches independently resettable in tests -- not a new abstraction.
10. **PASS** -- confirmed via `git diff` that neither `types/canvas.ts` nor `types/tasks.ts` appears in this diff. `chatHistory` is typed/validated as `AiChatMessage[]`/`AiChatMessageSchema` imported, not redefined.
11. **PASS** -- both `app/api/ai/spec/route.ts` and `app/api/ai/spec/token/route.ts` contain no Gemini/Trigger.dev-triggering logic beyond calling `triggerGenerateSpec`/`createRunToken`; all Gemini/Markdown-generation logic lives in `trigger/generate-spec.ts`/`lib/generate-spec-ai.ts`. No violation of Architecture Invariant 1.
12. **PASS** -- see Mechanical gate table above; independently reproduced, not taken on the Dev's word.

### Architecture invariants (`context/architecture-context.md`)

- Invariant 1 (no long-running AI work in a request handler) -- respected; verified above under criterion 11.
- Invariant 2 (metadata vs. artifact storage separation) -- respected; this unit stores only the `TaskRun` metadata row, no artifact write of any kind.
- Invariant 3 (auth/ownership enforced at every mutation) -- both routes gate on `getCallerIdentity`/`getProjectAccess`/`TaskRun.userId` before their one respective mutation (`taskRun.create`, token issuance).
- Invariant 4/5 -- not applicable to this backend-only unit.
- The new "Realtime Conventions" bullet in `architecture-context.md` accurately describes what `trigger/generate-spec.ts` actually does (`metadata.set`, no Liveblocks import) -- confirmed by reading the file: no `lib/design-agent-room.ts`/Liveblocks import anywhere in `trigger/generate-spec.ts`. Independently confirmed `metadata.set` is a real, callable export of the installed `@trigger.dev/sdk` package (not a speculative/fabricated API) via a direct `require()` check against `node_modules`.
- Confirmed the `createDesignRunToken` -> `createRunToken` rename in `lib/trigger.ts`/`app/api/ai/design/token/route.ts`/`app/api/ai/design/token/route.test.ts` is a byte-for-byte rename with no behavioral change (`git diff` reviewed directly) -- the pre-existing spec-22/23 design-agent token route still passes its full test suite unmodified in behavior.

### Standards compliance (`context/code-standards.md`)

- No `any` usage in any changed file (the only matches for the string "any" are inside English prose comments).
- No raw Tailwind color classes (`zinc-`/`slate-`) or hex literals anywhere in the diff -- expected, since this is a backend-only unit with no styling surface.
- No `components/ui/*` file touched.
- `interface` used for `GenerateSpecPayload`/`GenerateSpecResult`/`GenerateSpecGraphNode`/`GenerateSpecGraphEdge`/`GenerateSpecInput` per the brief's own explicit direction; Zod schemas kept as a separate, `.parse()`-enforced source of truth rather than silently drifting from the interfaces.
- Route handlers stay thin; Prisma/Trigger.dev/Clerk boundaries mocked correctly in every new test file per this repo's established `vi.mock`/`vi.hoisted` convention.

### Error handling

- Bad input: Zod `safeParse`/`.parse()` rejects malformed bodies/payloads at both the route and task boundary, before any side effect.
- Unauthorized: 401 checked first in both routes, before body parsing.
- Missing/foreign records: 404 for an unknown project or unknown `runId`; 403 for a non-member project or a `runId` owned by someone else.
- Upstream failures: a failed `triggerGenerateSpec`/`createRunToken` call returns 502 without a stray Prisma write; a failed `prisma.taskRun.create` after a successful trigger returns 500 (the run is already in flight at that point -- an accepted, pre-existing limitation shared with spec 22's design route, not something this spec regresses).
- Task-level failure: `runGenerateSpec`'s `catch` block publishes an `error` status via `metadata.set`, itself wrapped in a nested `try/catch` so a secondary metadata-publish failure can't mask the original error -- directly tested.

### Housekeeping

- `context/progress-tracker.md`'s "In Progress" entry for spec 27 (line 357) is largely accurate and detailed, but states "558/558 tests passing across 60 files" -- the actual, independently-reproduced count (and the Dev's own "Test coverage"/"Commands run" sections in this same file) is 58 files. Minor, non-blocking, but worth a one-line fix so the tracker doesn't drift from the number every other section of this same document already gets right.
- Everything else in `progress-tracker.md`'s spec-27 entry (files added/modified, the `createRunToken` generalization, the run-metadata decision, scope-limit confirmations) matches the actual diff.

### Issues found

1. **[Bug -> Dev] Stale file count in `context/progress-tracker.md`.** Line 357 ("In Progress" section) reads "558/558 tests passing across 60 files" -- the real count, confirmed by an independent `npx vitest run --no-file-parallelism` and by this same spec's own "Test coverage"/"Commands run" sections lower in this file, is 58 files. A one-line fix (60 files -> 58 files).

No other bugs or spec gaps found. This is a clean, well-scoped, thoroughly-tested implementation that faithfully follows the brief's own resolutions to all seven Open Questions, stays entirely inside its stated scope limits, and does not regress spec 22's already-shipped design-agent token route.

### Handoff

QA failed -- see issue above, routing to Dev. This is a single-line documentation fix only (`context/progress-tracker.md`); no code, test, or behavioral change is required. Given the triviality and zero risk of this fix, the Product Owner may wish to accept this as a non-blocking note rather than requiring a full Dev round-trip -- that call belongs to the Product Owner, not QA.

### Fix Applied

`context/progress-tracker.md` line 357 corrected from "558/558 tests passing across 60 files" to "558/558 tests passing across 58 files" -- a one-line, docs-only change, no code/test/behavioral change. No re-run of the mechanical gate was needed since nothing outside this one doc line changed. Proceeding directly to Product Owner review per QA's own note that a full re-review round is optional at the Product Owner's discretion for a fix this trivial.

## Product Owner Review (round 1)

**Verdict: PASS — ready for human review**

### Scope and diff verification (independent, not trusted from Dev/QA reports)

Reproduced `git diff main...HEAD --stat` directly rather than trusting either report's account: 15 files changed (5 new source files, 6 new/extended test files, `lib/trigger.ts`, `app/api/ai/design/token/route.ts` + its test, `context/architecture-context.md`, `context/progress-tracker.md`, this spec-status file). Confirmed by name-only diff and by grepping for the four scope-limited paths that no `components/*`, `prisma/schema.prisma`, `types/canvas.ts`, or `types/tasks.ts` file appears anywhere in the diff — the brief's own explicit Scope Limits and this spec's acceptance criteria 7/10 both hold.

Read the actual bodies of `app/api/ai/spec/route.ts`, `app/api/ai/spec/token/route.ts`, `lib/generate-spec-ai.ts`, `trigger/generate-spec.ts`, and the `lib/trigger.ts` diff directly (not summarized):

- `app/api/ai/spec/route.ts`'s `SpecRequestBodySchema` has no `projectId` field; `getProjectAccess(body.roomId)` and `triggerGenerateSpec({ projectId: body.roomId, roomId: body.roomId, ... })` both derive exclusively from the validated `roomId` — a client-supplied `projectId` is silently stripped by Zod's default object behavior before anything downstream sees it. Acceptance criterion 1 and the raw spec text's explicit "Do not trust a client-supplied `projectId`" instruction are both satisfied by construction, not by convention.
- `trigger/generate-spec.ts`'s `GenerateSpecPayloadSchema.parse(payload)` runs before the `try` block — a malformed payload never reaches Gemini and never publishes a misleading `start`/`processing` status. `generateSpecMarkdown` is genuinely called via `generateText` (not `generateObject`), and the task returns raw Markdown as its only output — no Blob write, no filesystem write, no second Prisma write beyond the route's own `TaskRun.create` (acceptance criteria 6, 8).
- `lib/generate-spec-ai.ts` reuses `design-agent-ai.ts`'s exact lazy-provider-instantiation shape with only a distinct `globalThis` cache key — genuinely not a new AI provider abstraction (acceptance criterion 9), and proactively applies spec 23's own live-verified `maxOutputTokens: 8192` lesson rather than waiting to rediscover it.
- `lib/trigger.ts`'s `createDesignRunToken` → `createRunToken` change is a pure rename/generalization — same body, same `"1h"` expiration constant (renamed, not changed), same single-run scope. Confirmed `app/api/ai/design/token/route.ts`'s diff is an import/call-site rename only, no behavioral change to spec 22's already-shipped token route.
- The new "Realtime Conventions" bullet in `context/architecture-context.md` accurately describes what the code does — `metadata.set`, no Liveblocks import anywhere in `trigger/generate-spec.ts`, confirmed by reading the file.

The uncommitted working-tree diff to `context/progress-tracker.md` at the time of this review is exactly the one-line "60 files" → "58 files" fix QA flagged — nothing else changed, matching the "Fix Applied" note above. Given QA's own explicit framing (a single-line, docs-only correction with zero code/test/behavioral impact, and an explicit note that a full Dev round-trip is optional at Product Owner discretion), accepting this as a non-blocking correction rather than sending it back to Dev for a formality is the right call — it would burn a pipeline round-trip for a change that has already been made correctly and verified above.

### Against `project-overview.md` Success Criteria

- **Success Criterion 5** ("The graph can be converted into a persisted Markdown spec") — this spec delivers the *generation* half genuinely, not just a technicality: unlike spec 22 (which shipped as a log-and-echo shell ahead of spec 23's real Gemini call), this spec ships the real Gemini-backed `generateText` call from day one, combining what specs 22/23 did separately for design-agent generation into one spec for spec-generation. A real, authenticated, access-checked `POST /api/ai/spec` call today produces genuine Gemini-drafted Markdown as a Trigger.dev task output — a substantive, non-technicality step, not merely satisfying the letter of the brief. The "persisted" half of Criterion 5 is explicitly and correctly deferred to spec 28 (Spec Persistence & Download) — this spec's own Scope Limit ("Do not store the final spec in this unit") and Out-of-scope callouts name spec 29/28 as the follow-ups, consistent with `ai-workflow-rules.md`'s incremental philosophy of shipping one coherent, independently-valuable slice at a time rather than bundling generation with persistence and UI in one pass.
- **Goal 4** ("Generation runs as a durable background task") and Architecture Invariant 1 ("Request handlers do not run long-lived AI work") — both routes stay thin; all Gemini/Markdown-generation logic lives only in `trigger/generate-spec.ts`/`lib/generate-spec-ai.ts`, confirmed directly in the route bodies above (acceptance criterion 11).
- No touches to any `project-overview.md` Out of Scope item (billing, enterprise tiers, versioned spec history, production object-storage migration, mobile) — this spec's own diff footprint doesn't come near any of them.

### Rough edges — acceptable at this stage, not a blocker

- **No live Gemini smoke test of `generateSpecMarkdown`'s `generateText` path specifically**, and no live Trigger.dev run of `generate-spec` triggered in this pass (Dev Notes' own "Known limitations" section). This is the same category of gap spec 22/23 shipped with before their own post-PASS live-verification rounds, and the design-agent's live-verified `gemini-3.6-flash`/`maxOutputTokens: 8192` lessons were already proactively applied here rather than needing to be rediscovered. Recommended as a human smoke test (trigger a real `generate-spec` run end to end and read back the Markdown from the Trigger.dev dashboard) before spec 28/29 build persistence/UI on top of it — not a blocker for this recommendation, and nothing about it would block spec 28 from building correctly on top of this spec's contracts (`{ runId }`, `{ token }`, and the task's `{ roomId, projectId, markdown }` output shape are all fully defined and unit-tested regardless of live verification status).
- **The run-metadata-vs-Liveblocks-broadcast choice (Open Questions #1)** is a genuine, non-trivial interpretive call the brief made and the Dev followed — resolved toward Trigger.dev's own `metadata.set`, consumable later via `@trigger.dev/react-hooks#useRealtimeRun` (already installed by spec 26). This is recorded as a shared architecture decision in `context/architecture-context.md` so spec 29 (or whichever spec wires the frontend) doesn't have to re-derive it, and is consistent with spec 26's own already-proven `useRealtimeRun` pattern for the design-agent run. No concern that this blocks correct downstream building.

### `progress-tracker.md` accuracy

The "In Progress" entry for spec 27 (post-fix, now reading "58 files") accurately reflects what was actually delivered: the two new routes' exact request/response shapes, the `generate-spec` task's real Gemini call and Zod validation, the `createRunToken` generalization, and the explicit confirmation that no `components/*`/`types/canvas.ts`/`types/tasks.ts`/`prisma/schema.prisma` file was touched. This matches QA's own independently-reproduced diff and mechanical-gate results, not an aspirational description. Moved to "Completed" below as part of this review, with "Current Phase"/"Next Up" advanced to spec 28 (Spec Persistence & Download), the next file in `context/feature-specs/`.

Ready for the human's final call on whether to move forward — this verdict is a recommendation, not a deployment authorization.
